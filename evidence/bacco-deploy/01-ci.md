# CI do fork — 2026-09-15

Plano 4, Task 1. Repo `github.com/lussandro/bacco-adega-crm` (privado). Regra: tag `v26.9.0` só depois de `ci` success.

## Workflows do fork

Restaurados da `v1.27.0` e **desligados no GitHub** (21 testes leem esses arquivos; apagar quebrava o `test:unit`):

```
.github/workflows/acolhida.yml disabled_manually
.github/workflows/e2e.yml      disabled_manually
.github/workflows/release.yml  disabled_manually
.github/workflows/relogio.yml  disabled_manually
.github/workflows/ci.yml       active
.github/workflows/perf.yml     active
.github/workflows/publish-image.yml active
```

O `release` disparou uma vez no push que o registrou (antes de dar tempo de desligar) e falhou sem os secrets do autor — sem efeito. Dependabot removido; PRs #1 e #2 fechados com comentário, execuções canceladas.

## Histórico medido do job `verify`

| Commit | Resultado | Causa | Correção |
|---|---|---|---|
| `336b9c97` (base, run 34979163506) | failure | Typecheck: `FATAL ERROR: Ineffective mark-compacts near heap limit … JavaScript heap out of memory`, exit 134 | `NODE_OPTIONS=--max-old-space-size=6144` no step Typecheck |
| `d51ef9a5` (run 34982794810) | failure | Typecheck 1m, Lint 1m04 ok; `Unit tests` cancelado após 12m30s — `The job has exceeded the maximum execution time of 15m0s` | `timeout-minutes: 45` no `verify` e razão escrita em `TETOS` de `tests/unit/preambulo-do-ci-nao-come-o-relogio.test.ts` |
| `26bdf24a` (run 34984735984) | failure | `Unit tests` 23m45s: `Test Files 887 passed`, `Tests 9063 passed \| 1 expected fail`, **`Errors 1 error`** → exit 1. `TeardownError: [vitest-worker]: Closing rpc while "onUserConsoleLog" was pending`, originado em `tests/unit/desfecho-de-agenda-e-sobre-o-passado.test.ts` | mock de `@/lib/audit` nesse arquivo (commit `d9887ebf`) |

Nos três, `invariants` (`test:db`) e `perf` passaram; `publish-image` passou nos pushes da `main`.

## Causa do erro não tratado (commit `26bdf24a`)

- `alterarAgendamentoHandler` chama `void audit(...)` (`app/api/v1/agenda/agendamentos/_handler.ts:383,390`).
- `audit()` usa `createAdminClient()`; no setup de teste o host é `.invalid`, e a falha só registra `console.error("[audit] insert error"…)` (`lib/audit/index.ts:114`) depois — com o teste já encerrado.
- O teste não mockava `@/lib/audit` (124 arquivos do repo mockam, forma `audit: vi.fn(async () => undefined)`).
- Upstream documentou o mesmo erro sob carga no PR #869 (só docs; arquivo passa isolado). No upstream, as últimas 11 execuções do `ci` na `main` (runner público) foram `success`.
- Outros testes unitários sem mock de `audit` foram varridos: nenhum com caminho provado até um `audit(` executado (os que importam handlers de inbox/contatos chamam listagens; três falsos positivos da varredura mockam o handler). Não foram tocados.

## `d9887ebf` (run 34988147368) — unit verde, kit vermelho

`Unit tests` success (15:26→15:44, sem erro não tratado: o mock resolveu). `invariants`, `perf`, `publish-image` success. `Kit self-host (bash)` failure em 27s — uma asserção só em `hostgator-setup-kit/test-validators.sh`:

```
packaging: a tag do git não basta — as imagens têm de existir
  ✗ com as imagens inalcançáveis, o instalador não avisou que ia construir aqui
FALHOU
```

Causa: o bloco não define `REPO_URL`; o install usa o padrão de `_common.sh:470`, que a Task 1 trocou de `github.com/melgarafael/DeskcommCRM.git` (público, com tags) para `github.com/lussandro/bacco-adega-crm.git` (privado, sem tags). `ultima_versao_publicada` volta vazio sem credencial, `VERSAO_ALVO` fica vazio e o install não entra no ramo `elif [ -n "$VERSAO_ALVO" ]` que imprime "construídas neste servidor" (`install.sh:1216-1223`). O teste dependia de rede e de repositório público — dependência implícita, não defeito do instalador.

Correção (commit `f52796ab`): o bloco cria origem git local com tag `v1.0.0` e exporta `REPO_URL` só em volta do `install.sh`, mesmo padrão do teste de pinagem vizinho ("Offline de propósito", `test-validators.sh:1790`). `bash -n` ok.

## `f52796ab` (run 34990752387) — verde

```
ci success
JOB invariants success   (RLS isolation + governance invariants 15:47:49→15:53:16)
JOB verify success
  Typecheck  15:47:35→15:48:37
  Lint       15:48:37→15:49:45
  Unit tests 15:49:46→16:12:43
  Kit self-host (bash) 16:12:43→16:13:12
perf success
Publicar imagem Docker (GHCR) success
```

Condição da spec §3.1 atendida. Tag `v26.9.0` criada no `f52796ab`.

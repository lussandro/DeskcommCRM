# CI e tag do rebrand — 2026-09-15

Plano 1 v2.1, Task 9. Push `2820a014..82fd7e52` para `main` do fork (`--no-tags`).

## Runs no SHA `82fd7e52228938e857b220a6d9a279da466ebe8e`

| Workflow | Run | Resultado |
|---|---|---|
| `ci` | `35002649235` | success |
| `perf` | `35002649205` | success |
| Publicar imagem Docker (GHCR) | `35002649263` | success |

Jobs:

```
invariants    success  17:39:42 → 17:48:17
verify        success  17:39:44 → 18:07:01
build-and-size success 17:39:43 → 17:43:48
a-tag-veio-da-main success
imagem-do-app-sobe success
build-and-push (deskcomm-scheduler) success
build-and-push (deskcommcrm)        success
build-and-push (deskcomm-worker)    success
imagens-ok    success
promover-stable skipped
```

Rodapé do `Unit tests` no log do `verify`:

```
 Test Files  887 passed (887)
      Tests  9083 passed | 1 expected fail (9084)
```

Sem linha `Errors`. O `verify` inclui o kit (`test:shell`), onde o caso
`APP_ACCENT_HEX inválido cai no accent do produto` agora espera `#4a0e1f`.

## Tag

```
$ git tag -a v26.9.1 -m "Bacco Adega CRM 26.9.1 — marca Bacco e funis de vinícola" 82fd7e52
$ git cat-file -t v26.9.1
tag
$ git push origin v26.9.1
 * [new tag]           v26.9.1 -> v26.9.1
```

Versão no esquema do fork (`vAA.M.P`): `release:conferir` calcula `1.28.0` sobre o CHANGELOG do
upstream, que o `sort -V` do `update.sh` poria abaixo da `v26.9.0` — não usado.

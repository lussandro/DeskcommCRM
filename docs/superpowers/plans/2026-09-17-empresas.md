# Empresas (cadastro com vários contatos) — Plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Entidade `crm_companies` no CRM, com N contatos por empresa e um contato principal para aviso de cobrança, sem mudar nada para quem não a usa.

**Architecture:** Tabela nova com RLS por tenant; `contacts.company_id` nullable; `crm_companies.billing_contact_id` nullable. API REST `/api/v1/companies` no mesmo molde de `contacts` (handler puro + route), duas telas (`/app/companies`, `/app/companies/[id]`), campo "Empresa" no diálogo de edição do contato. Código de empresas mora em `lib/companies/`, `app/api/v1/companies/`, `app/app/companies/`; fora dali só passa a coluna `company_id` como qualquer outra.

**Tech Stack:** Next.js 16 App Router, React 19, TypeScript estrito, Supabase (Postgres + RLS), Zod, react-query, Vitest, Playwright.

**Spec:** `docs/superpowers/specs/2026-09-17-bacco-asaas-design.md` §5.2a (Empresas) e tabela "restrição do dono". O módulo Asaas (plano separado) depende deste plano.

## Global Constraints

- **Tripla de migration:** arquivo `supabase/migrations/<timestamp>_0260_empresas.sql` + apêndice idempotente no fim de `supabase/baseline.sql` rotulado `-- ---- empresas (migration 0260, bacco) ----` + linha em `supabase/migrations/MANIFEST.md`. Próximo número: `ls supabase/migrations/ | grep -oE '_[0-9]{4}_' | tr -d _ | sort -n | tail -1` → `0259` hoje, então **0260**. O plano Asaas usa 0261.
- **Nada obrigatório:** `company_id` e `billing_contact_id` nullable; nenhum schema Zod existente ganha campo obrigatório.
- **Zero ramificação por empresa fora de `lib/companies/`, `app/api/v1/companies/`, `app/app/companies/`, `components/companies/`, `hooks/companies/` e `lib/asaas/`.** Passar a coluna `company_id` no SELECT/PATCH de contatos é permitido; importar `@/lib/companies` ou consultar `crm_companies` de `app/api/v1/{leads,conversations}`, `lib/{followup,routing,agent-engine,ai}` não é. Gate: `tests/unit/empresas-sao-aditivas.test.ts` (Task 9).
- **Função nova em `public` revoga `public` e `anon`** (CLAUDE.md, doutrina de migrations item 9).
- **`revoke all on <tabela> from anon`** em toda tabela nova (o default ACL do Supabase concede tudo a `anon`).
- **Texto de UI em português via `useT()`**, e **toda string nova entra em `lib/i18n/dicionario.ts` com a tradução em espanhol** — o gate `tests/unit/i18n-espanhol-cobre-a-tela.test.ts` varre `app` e `components` e reprova literal de `t()` sem entrada. A chave é o próprio texto em português.
- **Sem rate limit nas rotas de empresas.** Rotas cookie-auth de CRUD (contatos, leads) não têm; a spec pedia e foi corrigida para registrar a omissão. Entra quando existir caminho Bearer ou de importação em massa.
- **Gates que uma tela/tabela nova dispara** (rodar antes de dizer pronto): `navegacao-completude`, `telas-filtram-a-organizacao-ativa`, `telas-sem-dado-de-mentira`, `i18n-espanhol-cobre-a-tela`, `mapas-de-arquitetura`, `branding`, `rls-completude-varredura` (test:db), `lgpd-cascata-alcanca-quem-guarda-pessoa` (test:db), `migrations-nao-encolhem-vocabulario`, e `pnpm build`.
- **Sem `console.log`.** Sem env nova.
- **Commits em conventional commits, PT-BR**, um por task, com o rodapé de atribuição da sessão.
- **Prova:** `pnpm typecheck`, `pnpm lint`, `pnpm test:unit` (sem caminho) verdes; `pnpm test:db` roda na VPS (regra da conta: nada de Supabase local). Sem `test:db` rodado, a task de schema fica "não validada".

---

### Task 1: Migration 0260 (tabela, colunas, CHECK de `target_kind`, anonimização)

**Files:**
- Create: `supabase/migrations/20260917120000_0260_empresas.sql`
- Modify: `supabase/baseline.sql` (apêndice no fim)
- Modify: `supabase/migrations/MANIFEST.md` (última linha da tabela "Applied")
- Test: `tests/invariants/empresas.test.ts`

**Interfaces:**
- Produces: tabela `public.crm_companies(id, organization_id, name, trade_name, cnpj, asaas_customer_id, billing_contact_id, notes, created_by_user_id, created_at, updated_at)`; coluna `contacts.company_id uuid`; valor `'company'` em `crm_lead_links.target_kind`; trigger-irmão `trg_crm_companies_principal_anonimizado` (a spec dizia "sem novo trigger" e foi corrigida: um trigger a mais em `after update of is_anonymized` é o padrão do repo e não reescreve `fn_lgpd_anonymize_contact`); `fn_mesclar_contatos` ganha a regra de empresa (Task 8, mesma migration).

- [ ] **Step 1: Escrever a migration**

```sql
-- Empresas: o cliente pessoa jurídica com vários contatos (spec 2026-09-17-bacco-asaas-design §5.2a).
--
-- POR QUE: uma empresa tem mais de um telefone que fala com o financeiro. Sem a
-- entidade, o segundo telefone é um contato solto e a cobrança (módulo Asaas,
-- migration 0261) não sabe a quem pertence. `billing_contact_id` é o número
-- principal que recebe aviso de cobrança — decisão do dono; os demais contatos
-- só consultam quando eles mesmos escrevem.
--
-- ADITIVA: nada aqui é obrigatório. `contacts.company_id` é nullable, sem default
-- e sem trigger; quem não cadastra empresa não vê diferença. O único ponto
-- existente tocado é a anonimização LGPD, que passa a zerar o vínculo.

create table if not exists public.crm_companies (
  id uuid primary key default gen_random_uuid(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  name text not null,
  trade_name text,
  cnpj text,
  asaas_customer_id text,
  billing_contact_id uuid references public.contacts(id) on delete set null,
  notes text,
  created_by_user_id uuid references auth.users(id) on delete set null,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  constraint crm_companies_nome_nao_vazio check (length(btrim(name)) > 0),
  constraint crm_companies_cnpj_digitos check (cnpj is null or cnpj ~ '^[0-9]{14}$')
);

create unique index if not exists uq_crm_companies_org_cnpj
  on public.crm_companies (organization_id, cnpj) where cnpj is not null;
create unique index if not exists uq_crm_companies_org_asaas_customer
  on public.crm_companies (organization_id, asaas_customer_id) where asaas_customer_id is not null;
create index if not exists idx_crm_companies_org_name
  on public.crm_companies (organization_id, lower(name));

alter table public.crm_companies enable row level security;

drop policy if exists tenant_isolation_crm_companies_all on public.crm_companies;
create policy tenant_isolation_crm_companies_all on public.crm_companies
  using (organization_id in (select public.fn_user_org_ids()))
  with check (organization_id in (select public.fn_user_org_ids()));

revoke all on public.crm_companies from anon;
grant select, insert, update, delete on public.crm_companies to authenticated;
grant all on public.crm_companies to service_role;

drop trigger if exists trg_crm_companies_updated_at on public.crm_companies;
create trigger trg_crm_companies_updated_at
  before update on public.crm_companies
  for each row execute function public.fn_set_updated_at();

comment on table public.crm_companies is
  'Empresa cliente (pessoa jurídica) com N contatos. billing_contact_id = o contato que recebe aviso de cobrança (módulo Asaas). Aditiva: nada depende dela.';

-- O contato aponta a empresa. Nullable, sem default, sem trigger.
alter table public.contacts add column if not exists company_id uuid
  references public.crm_companies(id) on delete set null;
create index if not exists idx_contacts_org_company
  on public.contacts (organization_id, company_id) where company_id is not null;

-- A oportunidade pode apontar a empresa pelo vínculo polimórfico (DIRC: Referenciar).
-- ⚠️ CHECK recriado inteiro (mesmo formato da 0242): `add constraint` não é idempotente.
alter table public.crm_lead_links drop constraint if exists crm_lead_links_target_kind_enum;
alter table public.crm_lead_links add constraint crm_lead_links_target_kind_enum
  check (target_kind in ('order','conversation','message','appointment','contact','lead','external','company'));

-- Anonimizar o contato zera o vínculo com a empresa (a empresa não é pessoa; fica).
-- Se ele era o número principal, a empresa fica sem principal — o módulo Asaas
-- abre aviso na Central na próxima vencida em vez de escolher outro no chute.
create or replace function public.fn_empresa_solta_principal_anonimizado()
returns trigger
language plpgsql
security definer
set search_path = public, pg_temp
as $$
begin
  update public.crm_companies
     set billing_contact_id = null, updated_at = now()
   where organization_id = new.organization_id
     and billing_contact_id = new.id;
  update public.contacts
     set company_id = null
   where id = new.id and company_id is not null;
  return new;
end;
$$;
revoke execute on function public.fn_empresa_solta_principal_anonimizado() from public, anon, authenticated;
grant execute on function public.fn_empresa_solta_principal_anonimizado() to service_role;

drop trigger if exists trg_crm_companies_principal_anonimizado on public.contacts;
create trigger trg_crm_companies_principal_anonimizado
  after update of is_anonymized on public.contacts
  for each row
  when (new.is_anonymized is true and old.is_anonymized is distinct from true)
  execute function public.fn_empresa_solta_principal_anonimizado();

-- O PostgREST cacheia o schema; sem isto a rota que roda logo depois não vê a tabela.
notify pgrst, 'reload schema';
```

Por que trigger e não editar `fn_lgpd_anonymize_contact`: a função é grande, `security definer`, e o repo já usa o padrão de triggers-irmãos em `after update of is_anonymized` (`trg_contacts_anonimizado_limpa_propostas`, `trg_contacts_anonimizado_limpa_custom_fields`, `trg_redigir_tarefas_ao_anonimizar`). Um trigger a mais no mesmo evento é a mudança menor e não reescreve o núcleo.

- [ ] **Step 2: Apêndice no `baseline.sql`**

Ao **fim** de `supabase/baseline.sql`, colar o conteúdo inteiro da migration precedido de:

```sql
-- ---- empresas (migration 0260, bacco) ----
```

Tudo já é idempotente (`if not exists`, `drop ... if exists` + `create`, `create or replace`).

- [ ] **Step 3: Linha no MANIFEST**

Acrescentar ao fim da tabela "Applied" de `supabase/migrations/MANIFEST.md`:

```
| `20260917120000` | `0260_empresas` | Empresas (`crm_companies`) com N contatos e `billing_contact_id` (o número principal para aviso de cobrança); `contacts.company_id` nullable; `'company'` em `crm_lead_links.target_kind` (CHECK recriado inteiro); trigger que solta o vínculo ao anonimizar o contato. Aditiva: nada existente passa a depender dela. Spec `docs/superpowers/specs/2026-09-17-bacco-asaas-design.md` §5.2a. |
```

- [ ] **Step 4: Teste de invariante (roda com `pnpm test:db`)**

`tests/invariants/empresas.test.ts`. `sql` e `countAs` **já são exportados** de `tests/invariants/gov-helpers.ts:23,57` — importar de lá. `seedOrg` e as constantes `ORG_A/ORG_B/USER_A/USER_B` são locais de `tests/invariants/rls-isolation.test.ts:47-73` — **extrair só esses** para `tests/invariants/rls-isolation.helpers.ts` e fazer o teste antigo importar dali (refatoração mecânica; a suíte antiga tem de continuar verde). O plano Asaas importa do mesmo arquivo. ⚠️ `seedOrg(...)` **devolve SQL, não executa** — o uso é `sql(seedOrg(...) + seedOrg(...))`.

Três arquivos existentes ganham uma linha cada, **no mesmo commit**, senão `test:db` fica vermelho:

- `tests/invariants/rls-completude-varredura.test.ts`, em `PROVA_PROPRIA`: `{ tabela: "crm_companies", razao: "tests/invariants/empresas.test.ts — dois tenants, leitura negativa cruzada por JWT; billing_contact_id solto na anonimização" }`.
- `tests/invariants/lgpd-cascata-alcanca-quem-guarda-pessoa.test.ts`, em `DIVIDA_LGPD_CONHECIDA`: `crm_companies: "Pessoa jurídica: name/cnpj/notes são da empresa, não do titular. A FK billing_contact_id é solta pelo trigger trg_crm_companies_principal_anonimizado; não há dado pessoal a redigir aqui."` (≥ 40 caracteres, como o teste exige).
- `tests/invariants/rls-isolation.test.ts` não muda (a tabela entra por `PROVA_PROPRIA`).

```ts
import { describe, it, expect, beforeAll } from "vitest";
// helpers: mesmos de rls-isolation.test.ts
import { sql, countAs } from "./gov-helpers";
import { seedOrg, ORG_A, ORG_B, USER_A, USER_B } from "./rls-isolation.helpers";

const EMP_A = "aaaaaaaa-0000-0000-0000-00000000c0a1";
const EMP_B = "bbbbbbbb-0000-0000-0000-00000000c0b1";
const CT_A1 = "aaaaaaaa-0000-0000-0000-00000000cca1";
const CT_A2 = "aaaaaaaa-0000-0000-0000-00000000cca2";

describe("empresas (migration 0260)", () => {
  beforeAll(() => {
    sql(seedOrg(ORG_A, USER_A, "sess-a", "a") + seedOrg(ORG_B, USER_B, "sess-b", "b"));
    sql(`
      insert into public.crm_companies (id, organization_id, name, cnpj) values
        ('${EMP_A}', '${ORG_A}', 'Adega A', '11222333000181'),
        ('${EMP_B}', '${ORG_B}', 'Adega B', '11222333000262')
      on conflict do nothing;
      insert into public.contacts (id, organization_id, name, phone_number, company_id) values
        ('${CT_A1}', '${ORG_A}', 'Fin 1', '+5551999990001', '${EMP_A}'),
        ('${CT_A2}', '${ORG_A}', 'Fin 2', '+5551999990002', '${EMP_A}')
      on conflict do nothing;
      update public.crm_companies set billing_contact_id = '${CT_A1}' where id = '${EMP_A}';
    `);
  });

  it("RLS: org B não lê a empresa de A, e lê a própria", () => {
    expect(countAs(USER_B, `select count(*) from public.crm_companies where organization_id = '${ORG_A}';`)).toBe(0);
    expect(countAs(USER_B, `select count(*) from public.crm_companies where organization_id = '${ORG_B}';`)).toBe(1);
  });

  it("revoke de anon segura mesmo sob o default ACL do Supabase", () => {
    // O Postgres do test-db não reproduz o default ACL de TABELAS do Supabase (só o de
    // funções) — sem este grant prévio o caso ficaria verde com o revoke ausente.
    sql(`grant all on public.crm_companies to anon;`);
    sql(`revoke all on public.crm_companies from anon;`); // o mesmo comando do apêndice
    const out = sql(`select count(*) from information_schema.role_table_grants
      where table_schema='public' and table_name='crm_companies' and grantee='anon';`);
    expect(out.trim()).toBe("0");
  });

  it("CNPJ só dígitos e único por org", () => {
    expect(() => sql(`insert into public.crm_companies (organization_id, name, cnpj) values ('${ORG_A}','X','11.222.333/0001-81');`)).toThrow();
    expect(() => sql(`insert into public.crm_companies (organization_id, name, cnpj) values ('${ORG_A}','Y','11222333000181');`)).toThrow();
    // a mesma sequência noutra org passa
    sql(`insert into public.crm_companies (organization_id, name, cnpj) values ('${ORG_B}','Z','11222333000181');`);
  });

  it("target_kind tem exatamente os sete antigos + company", () => {
    const out = sql(`select pg_get_constraintdef(oid) from pg_constraint where conname='crm_lead_links_target_kind_enum';`);
    const valores = [...out.matchAll(/'([a-z_]+)'/g)].map((m) => m[1]).sort();
    expect(valores).toEqual(["appointment","company","contact","conversation","external","lead","message","order"]);
  });

  it("anonimizar o principal solta o vínculo e o contato sai da empresa", () => {
    sql(`update public.contacts set is_anonymized = true, anonymized_at = now() where id = '${CT_A1}';`);
    expect(sql(`select coalesce(billing_contact_id::text,'null') from public.crm_companies where id='${EMP_A}';`).trim()).toBe("null");
    expect(sql(`select coalesce(company_id::text,'null') from public.contacts where id='${CT_A1}';`).trim()).toBe("null");
    // o outro contato continua na empresa
    expect(sql(`select company_id::text from public.contacts where id='${CT_A2}';`).trim()).toBe(EMP_A);
  });

  it("apagar a empresa não apaga contatos", () => {
    sql(`delete from public.crm_companies where id='${EMP_A}';`);
    expect(sql(`select count(*) from public.contacts where id in ('${CT_A1}','${CT_A2}');`).trim()).toBe("2");
  });
});
```

`rls-isolation.helpers.ts` nasce nesta task (ver acima); `sql`/`countAs` não se duplicam.

- [ ] **Step 5: Rodar `pnpm test:db` na VPS**

Regra da conta: nada local. Na VPS (ver memória `testes-na-vps-nao-local`), no clone do repo com Docker:

```bash
pnpm test:db > /tmp/tdb.log 2>&1; echo "exit=$?"; grep -aE "Test Files|Tests |Errors " /tmp/tdb.log | tail -3
```

Esperado: `exit=0`, `empresas.test.ts` com 7 passed (6 daqui + o de merge da Task 8, que entra na mesma migration), `install` e `update` do baseline verdes, `rls-completude-varredura`, `lgpd-cascata-alcanca-quem-guarda-pessoa`, `migrations-nao-encolhem-vocabulario` e `baseline-constraint-reconstruida` verdes.

- [ ] **Step 6: Regenerar tipos**

Não há script `gen:types` (a doc que o cita está desatualizada). Com a migration aplicada no Supabase da VPS:

```bash
supabase gen types typescript --db-url "$SUPABASE_DB_URL" > lib/database.types.ts   # sem login; lê o banco da VPS
git diff --stat lib/database.types.ts
```

O diff pode trazer drift antigo além de `crm_companies`/`company_id` — se trouxer, é sinal de que o arquivo já estava defasado; aceitar o regen inteiro e citar no commit. Consumidores tipados: `lib/routing/{eligibles,decide}.ts`, `lib/ai/dispatcher/index.ts`, `lib/lgpd/export-collector.ts`, `lib/agenda/google/sync-store.ts` — `pnpm typecheck` decide.

- [ ] **Step 7: Commit**

```bash
git add supabase/migrations/20260917120000_0260_empresas.sql supabase/baseline.sql supabase/migrations/MANIFEST.md tests/invariants/empresas.test.ts lib/database.types.ts
git commit -m "feat(bacco): empresas — tabela crm_companies, contacts.company_id e principal de cobrança (migration 0260)"
```

---

### Task 2: Tipos, schemas Zod e handler puro de empresas

**Files:**
- Create: `lib/types/companies.ts`
- Create: `lib/schemas/companies.ts`
- Modify: `lib/schemas/index.ts` (reexport)
- Create: `lib/companies/handler.ts`
- Test: `lib/companies/handler.test.ts`

**Interfaces:**
- Produces:
  - `interface Company { id; organization_id; name; trade_name: string|null; cnpj: string|null; asaas_customer_id: string|null; billing_contact_id: string|null; notes: string|null; created_at; updated_at; contacts_count?: number }`
  - `companyCreateSchema`, `companyPatchSchema`, `companyListQuerySchema`, `companyLinkContactSchema`
  - `listCompaniesHandler(supabase, ctx, query)`, `getCompanyHandler(supabase, ctx, { companyId })`, `createCompanyHandler(supabase, ctx, input)`, `patchCompanyHandler(supabase, ctx, { companyId, patch })`, `deleteCompanyHandler(supabase, ctx, { companyId })`, `linkContactHandler(supabase, ctx, { companyId, contactId })`, `unlinkContactHandler(supabase, ctx, { companyId, contactId })` — todos com `ctx: HandlerCtx` (`@/lib/api/handlers/types`), lançam `ApiError`, auditam.

- [ ] **Step 1: Tipos e schemas**

`lib/types/companies.ts`:

```ts
export interface Company {
  id: string;
  organization_id: string;
  name: string;
  trade_name: string | null;
  cnpj: string | null;
  asaas_customer_id: string | null;
  billing_contact_id: string | null;
  notes: string | null;
  created_at: string;
  updated_at: string;
  /** Derivado no GET de lista/detalhe. */
  contacts_count?: number;
}

export interface CompanyContact {
  id: string;
  name: string | null;
  display_name: string | null;
  phone_number: string | null;
  email: string | null;
  is_billing_contact: boolean;
}
```

`lib/schemas/companies.ts`:

```ts
import { z } from "zod";

/** Só dígitos; a máscara é da tela. Validação de dígito verificador fica para quando alguém pedir. */
export const cnpjSchema = z
  .string()
  .transform((s) => s.replace(/\D/g, ""))
  .refine((s) => s.length === 14, "CNPJ precisa ter 14 dígitos");

export const companyCreateSchema = z.object({
  name: z.string().trim().min(1).max(200),
  trade_name: z.string().trim().min(1).max(200).nullable().optional(),
  cnpj: cnpjSchema.nullable().optional(),
  notes: z.string().max(4000).nullable().optional(),
});
export type CompanyCreate = z.infer<typeof companyCreateSchema>;

export const companyPatchSchema = companyCreateSchema.partial().extend({
  billing_contact_id: z.string().uuid().nullable().optional(),
});
export type CompanyPatch = z.infer<typeof companyPatchSchema>;

export const companyListQuerySchema = z.object({
  search: z.string().trim().max(200).optional(),
  limit: z.coerce.number().int().min(1).max(100).default(25),
  cursor: z.string().optional(),
});
export type CompanyListQuery = z.infer<typeof companyListQuerySchema>;

export const companyLinkContactSchema = z.object({
  contact_id: z.string().uuid(),
});
```

Em `lib/schemas/index.ts` acrescentar `export * from "./companies";`.

Em `lib/schemas/contacts.ts`, nos dois schemas (`contactCreateSchema` e, por herança, `contactPatchSchema`), acrescentar **um campo opcional**:

```ts
  company_id: z.string().uuid().nullable().optional(),
```

- [ ] **Step 2: Teste do handler (falhando)**

`lib/companies/handler.test.ts` — mock de supabase no estilo de `app/api/v1/contacts/route.test.ts` (query builder encadeado). Casos:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import { ApiError } from "@/lib/api/types";

vi.mock("@/lib/audit", () => ({ audit: vi.fn(async () => undefined) }));
import { audit } from "@/lib/audit";

const ORG = "11111111-1111-4111-8111-111111111111";
const OUTRA = "22222222-2222-4222-8222-222222222222";
const ctx = { organization_id: ORG, actor: { type: "user" as const, id: "u1" }, requestId: "r1", idioma: "pt-BR" };

/** Builder mínimo: grava os filtros e devolve o que `resultado` mandar. */
function fakeSb(resultado: { data: unknown; error: unknown }) {
  const calls: Record<string, unknown[]> = {};
  const b: Record<string, unknown> = {};
  for (const m of ["from","select","eq","ilike","or","order","limit","insert","update","delete","maybeSingle","single","in","is"]) {
    b[m] = vi.fn((...a: unknown[]) => { (calls[m] ??= []).push(a); return m === "maybeSingle" || m === "single" ? Promise.resolve(resultado) : b; });
  }
  (b as { then?: unknown }).then = (res: (v: unknown) => void) => res(resultado);
  return { sb: b as never, calls };
}

describe("createCompanyHandler", () => {
  beforeEach(() => vi.clearAllMocks());

  it("grava com organization_id do ctx (nunca do input) e audita company.created", async () => {
    const { createCompanyHandler } = await import("./handler");
    const linha = { id: "c1", organization_id: ORG, name: "Adega", trade_name: null, cnpj: "11222333000181", asaas_customer_id: null, billing_contact_id: null, notes: null, created_at: "", updated_at: "" };
    const { sb, calls } = fakeSb({ data: linha, error: null });
    const out = await createCompanyHandler(sb, ctx, { name: "Adega", cnpj: "11222333000181", organization_id: OUTRA } as never);
    expect(out.id).toBe("c1");
    expect((calls.insert![0] as unknown[])[0]).toMatchObject({ organization_id: ORG, name: "Adega" });
    expect(vi.mocked(audit).mock.calls[0]![0]).toMatchObject({ action: "company.created", organizationId: ORG, resourceId: "c1" });
  });

  it("CNPJ repetido vira 409 cnpj_em_uso com a mensagem para o usuário", async () => {
    const { createCompanyHandler } = await import("./handler");
    const { sb } = fakeSb({ data: null, error: { code: "23505", message: "uq_crm_companies_org_cnpj" } });
    await expect(createCompanyHandler(sb, ctx, { name: "X", cnpj: "11222333000181" })).rejects.toMatchObject({ status: 409, code: "cnpj_em_uso" });
  });
});

describe("listCompaniesHandler", () => {
  it("busca com vírgula e parêntese não injeta condição no .or()", async () => {
    const { listCompaniesHandler } = await import("./handler");
    const { sb, calls } = fakeSb({ data: [], error: null });
    await listCompaniesHandler(sb, ctx, { search: "Adega, (Sul)", limit: 25 });
    const expr = String((calls.or![0] as unknown[])[0]);
    expect(expr).not.toMatch(/[()]{2}|,,/);
    expect(expr).toContain("Adega  Sul");
  });
  it("cursor é JSON base64url com name e id, e nome com espaço volta inteiro", async () => {
    const { listCompaniesHandler } = await import("./handler");
    const linhas = Array.from({ length: 3 }, (_, i) => ({ id: `c${i}`, organization_id: ORG, name: `Adega ${i}`, contacts: [{ count: 0 }] }));
    const { sb } = fakeSb({ data: linhas, error: null });
    const out = await listCompaniesHandler(sb, ctx, { limit: 2 });
    expect(out.has_more).toBe(true);
    expect(JSON.parse(Buffer.from(out.cursor!, "base64url").toString("utf8"))).toEqual({ name: "Adega 1", id: "c1" });
  });
});

describe("patchCompanyHandler — billing_contact_id", () => {
  it("recusa contato que não pertence à empresa (422 principal_fora_da_empresa)", async () => {
    const { patchCompanyHandler } = await import("./handler");
    // primeira consulta: o contato existe mas company_id é outra empresa
    const { sb } = fakeSb({ data: { id: "ct1", company_id: "outra" }, error: null });
    await expect(patchCompanyHandler(sb, ctx, { companyId: "c1", patch: { billing_contact_id: "ct1" } })).rejects.toMatchObject({ status: 422, code: "principal_fora_da_empresa" });
  });
});

describe("linkContactHandler", () => {
  it("grava company_id no contato filtrando organization_id e audita company.contact_linked", async () => {
    const { linkContactHandler } = await import("./handler");
    const { sb, calls } = fakeSb({ data: { id: "ct1", company_id: "c1" }, error: null });
    await linkContactHandler(sb, ctx, { companyId: "c1", contactId: "ct1" });
    expect((calls.update![0] as unknown[])[0]).toEqual({ company_id: "c1" });
    expect(calls.eq!.flat()).toEqual(expect.arrayContaining(["organization_id", ORG, "id", "ct1"]));
    expect(vi.mocked(audit).mock.calls.at(-1)![0]).toMatchObject({ action: "company.contact_linked" });
  });
});
```

- [ ] **Step 3: Rodar e ver falhar**

```bash
pnpm vitest run lib/companies/handler.test.ts
```

Esperado: FAIL — `Cannot find module './handler'`.

- [ ] **Step 4: Implementar o handler**

`lib/companies/handler.ts`:

```ts
/**
 * Handlers puros de Empresas — reusados pela rota REST e (no módulo Asaas) pelo
 * consumidor de webhook. Recebem `HandlerCtx`, lançam `ApiError`, auditam.
 * `organization_id` vem SEMPRE do ctx; o input nunca o carrega.
 */
import type { SupabaseClient } from "@supabase/supabase-js";
import { ApiError } from "@/lib/api/types";
import type { HandlerCtx } from "@/lib/api/handlers/types";
import { audit } from "@/lib/audit";
import { traduzir } from "@/lib/i18n/dicionario";
import type { Company, CompanyContact } from "@/lib/types/companies";
import type { CompanyCreate, CompanyPatch, CompanyListQuery } from "@/lib/schemas/companies";

type SB = SupabaseClient;

const COLS =
  "id, organization_id, name, trade_name, cnpj, asaas_customer_id, billing_contact_id, notes, created_at, updated_at";
const CONTACT_COLS = "id, name, display_name, phone_number, email";

function atorAudit(ctx: HandlerCtx) {
  return ctx.actor.type === "user"
    ? { actorUserId: ctx.actor.id, metadataActor: {} }
    : { actorUserId: null, metadataActor: { actor_type: ctx.actor.type, actor_id: ctx.actor.id } };
}

function erroBanco(ctx: HandlerCtx, error: { code?: string; message: string }): never {
  if (error.code === "23505" && error.message.includes("uq_crm_companies_org_cnpj")) {
    throw new ApiError(409, "cnpj_em_uso", undefined, ctx.requestId,
      traduzir("Já existe uma empresa com este CNPJ nesta organização.", ctx.idioma ?? "pt-BR"));
  }
  throw new ApiError(500, "internal_error", undefined, ctx.requestId, error.message);
}

export async function listCompaniesHandler(
  supabase: SB,
  ctx: HandlerCtx,
  query: CompanyListQuery,
): Promise<{ companies: Company[]; cursor: string | null; has_more: boolean }> {
  let q = supabase
    .from("crm_companies")
    .select(`${COLS}, contacts:contacts(count)`)
    .eq("organization_id", ctx.organization_id)
    .order("name", { ascending: true })
    .order("id", { ascending: true })
    .limit(query.limit + 1);
  if (query.search) {
    // `%`/`_` são curingas do LIKE; `,()` são delimitadores do DSL do `.or()` — mesmo
    // escape de contacts/_handler.ts:125-131 ("Silva, Maria" injetaria condição).
    const s = query.search.trim().replace(/[%_]/g, (m) => `\\${m}`).replace(/[,()]/g, " ");
    const digitos = query.search.replace(/\D/g, "");
    q = digitos.length >= 4
      ? q.or(`name.ilike.%${s}%,trade_name.ilike.%${s}%,cnpj.ilike.%${digitos}%`)
      : q.or(`name.ilike.%${s}%,trade_name.ilike.%${s}%`);
  }
  const cursor = query.cursor ? decodeCursor(query.cursor) : null;
  if (cursor) {
    // Paginação por (name, id). O nome entra no DSL entre aspas e sem `,()"` — o
    // mesmo cuidado da busca; o id é uuid e não precisa de escape.
    const nome = cursor.name.replace(/[,()"]/g, " ");
    q = q.or(`name.gt."${nome}",and(name.eq."${nome}",id.gt.${cursor.id})`);
  }
  const { data, error } = await q;
  if (error) erroBanco(ctx, error);
  const rows = (data ?? []) as Array<Company & { contacts: Array<{ count: number }> }>;
  const has_more = rows.length > query.limit;
  const page = rows.slice(0, query.limit).map(({ contacts, ...c }) => ({ ...c, contacts_count: contacts?.[0]?.count ?? 0 }));
  const last = page.at(-1);
  const proximo = has_more && last ? encodeCursor({ name: last.name, id: last.id }) : null;
  return { companies: page, cursor: proximo, has_more };
}

interface CursorPayload { name: string; id: string }
function encodeCursor(p: CursorPayload): string {
  return Buffer.from(JSON.stringify(p), "utf8").toString("base64url");
}
function decodeCursor(raw: string): CursorPayload | null {
  try {
    const p = JSON.parse(Buffer.from(raw, "base64url").toString("utf8")) as Partial<CursorPayload>;
    return typeof p.name === "string" && typeof p.id === "string" ? { name: p.name, id: p.id } : null;
  } catch {
    return null;
  }
}

export async function getCompanyHandler(
  supabase: SB,
  ctx: HandlerCtx,
  input: { companyId: string },
): Promise<Company & { contacts: CompanyContact[] }> {
  const { data, error } = await supabase
    .from("crm_companies")
    .select(COLS)
    .eq("organization_id", ctx.organization_id)
    .eq("id", input.companyId)
    .maybeSingle();
  if (error) erroBanco(ctx, error);
  if (!data) {
    throw new ApiError(404, "not_found", undefined, ctx.requestId,
      traduzir("Empresa não encontrada.", ctx.idioma ?? "pt-BR"));
  }
  const empresa = data as Company;
  const { data: cts, error: e2 } = await supabase
    .from("contacts")
    .select(CONTACT_COLS)
    .eq("organization_id", ctx.organization_id)
    .eq("company_id", empresa.id)
    .eq("is_anonymized", false)
    .is("is_merged_into", null)
    .order("name", { ascending: true });
  if (e2) erroBanco(ctx, e2);
  const contacts = ((cts ?? []) as Array<Omit<CompanyContact, "is_billing_contact">>).map((c) => ({
    ...c,
    is_billing_contact: c.id === empresa.billing_contact_id,
  }));
  return { ...empresa, contacts_count: contacts.length, contacts };
}

export async function createCompanyHandler(supabase: SB, ctx: HandlerCtx, input: CompanyCreate): Promise<Company> {
  const { data, error } = await supabase
    .from("crm_companies")
    .insert({
      organization_id: ctx.organization_id,
      name: input.name,
      trade_name: input.trade_name ?? null,
      cnpj: input.cnpj ?? null,
      notes: input.notes ?? null,
      created_by_user_id: ctx.actor.type === "user" ? ctx.actor.id : null,
    })
    .select(COLS)
    .single();
  if (error || !data) erroBanco(ctx, error ?? { message: "insert sem linha" });
  const empresa = data as Company;
  const a = atorAudit(ctx);
  await audit({
    action: "company.created",
    actorUserId: a.actorUserId,
    organizationId: ctx.organization_id,
    resourceType: "company",
    resourceId: empresa.id,
    requestId: ctx.requestId,
    metadata: { ...a.metadataActor, has_cnpj: !!empresa.cnpj },
  });
  return empresa;
}

export async function patchCompanyHandler(
  supabase: SB,
  ctx: HandlerCtx,
  input: { companyId: string; patch: CompanyPatch },
): Promise<Company> {
  if (input.patch.billing_contact_id) {
    const { data: ct, error } = await supabase
      .from("contacts")
      .select("id, company_id")
      .eq("organization_id", ctx.organization_id)
      .eq("id", input.patch.billing_contact_id)
      .maybeSingle();
    if (error) erroBanco(ctx, error);
    if (!ct || (ct as { company_id: string | null }).company_id !== input.companyId) {
      throw new ApiError(422, "principal_fora_da_empresa", undefined, ctx.requestId,
        traduzir("O número principal precisa ser um contato desta empresa.", ctx.idioma ?? "pt-BR"));
    }
  }
  const { data, error } = await supabase
    .from("crm_companies")
    .update(input.patch)
    .eq("organization_id", ctx.organization_id)
    .eq("id", input.companyId)
    .select(COLS)
    .maybeSingle();
  if (error) erroBanco(ctx, error);
  if (!data) {
    throw new ApiError(404, "not_found", undefined, ctx.requestId,
      traduzir("Empresa não encontrada.", ctx.idioma ?? "pt-BR"));
  }
  const a = atorAudit(ctx);
  await audit({
    action: "company.updated",
    actorUserId: a.actorUserId,
    organizationId: ctx.organization_id,
    resourceType: "company",
    resourceId: input.companyId,
    requestId: ctx.requestId,
    metadata: { ...a.metadataActor, fields: Object.keys(input.patch) },
  });
  return data as Company;
}

export async function deleteCompanyHandler(supabase: SB, ctx: HandlerCtx, input: { companyId: string }): Promise<void> {
  const { data, error } = await supabase
    .from("crm_companies")
    .delete()
    .eq("organization_id", ctx.organization_id)
    .eq("id", input.companyId)
    .select("id")
    .maybeSingle();
  if (error) erroBanco(ctx, error);
  if (!data) {
    throw new ApiError(404, "not_found", undefined, ctx.requestId,
      traduzir("Empresa não encontrada.", ctx.idioma ?? "pt-BR"));
  }
  const a = atorAudit(ctx);
  await audit({
    action: "company.deleted",
    actorUserId: a.actorUserId,
    organizationId: ctx.organization_id,
    resourceType: "company",
    resourceId: input.companyId,
    requestId: ctx.requestId,
    metadata: a.metadataActor,
  });
}

async function mudarVinculo(
  supabase: SB,
  ctx: HandlerCtx,
  input: { companyId: string; contactId: string },
  novo: string | null,
  action: "company.contact_linked" | "company.contact_unlinked",
): Promise<void> {
  const { data, error } = await supabase
    .from("contacts")
    .update({ company_id: novo })
    .eq("organization_id", ctx.organization_id)
    .eq("id", input.contactId)
    .select("id, company_id")
    .maybeSingle();
  if (error) erroBanco(ctx, error);
  if (!data) {
    throw new ApiError(404, "not_found", undefined, ctx.requestId,
      traduzir("Contato não encontrado.", ctx.idioma ?? "pt-BR"));
  }
  const a = atorAudit(ctx);
  await audit({
    action,
    actorUserId: a.actorUserId,
    organizationId: ctx.organization_id,
    resourceType: "company",
    resourceId: input.companyId,
    requestId: ctx.requestId,
    metadata: { ...a.metadataActor, contact_id: input.contactId },
  });
}

export function linkContactHandler(supabase: SB, ctx: HandlerCtx, input: { companyId: string; contactId: string }) {
  return mudarVinculo(supabase, ctx, input, input.companyId, "company.contact_linked");
}

/** Desvincular o principal também zera `billing_contact_id` — o trigger do banco não cobre este caminho (só anonimização). */
export async function unlinkContactHandler(supabase: SB, ctx: HandlerCtx, input: { companyId: string; contactId: string }) {
  await mudarVinculo(supabase, ctx, input, null, "company.contact_unlinked");
  await supabase
    .from("crm_companies")
    .update({ billing_contact_id: null })
    .eq("organization_id", ctx.organization_id)
    .eq("id", input.companyId)
    .eq("billing_contact_id", input.contactId);
}
```

Conferir que `traduzir` existe em `lib/i18n/traduzir` (é o que `contacts/_handler.ts` importa; se o caminho for outro, usar o mesmo do handler de contatos).

- [ ] **Step 5: Ações de audit**

Em `lib/audit/actions.ts`, dentro de `AUDIT_ACTIONS`, logo após o bloco de `contact.*`:

```ts
  // Empresas (spec 2026-09-17-bacco-asaas-design §5.2a)
  "company.created",
  "company.updated",
  "company.deleted",
  "company.contact_linked",
  "company.contact_unlinked",
```

O arquivo continua sem `import` (gate `audit-lista-do-painel-e-derivada`).

- [ ] **Step 6: Rodar e ver passar**

```bash
pnpm vitest run lib/companies/handler.test.ts tests/unit/audit-lista-do-painel-e-derivada.test.tsx
```

Esperado: PASS (6 + os do painel).

- [ ] **Step 7: Commit**

```bash
git add lib/types/companies.ts lib/schemas/companies.ts lib/schemas/index.ts lib/schemas/contacts.ts lib/companies/handler.ts lib/companies/handler.test.ts lib/audit/actions.ts
git commit -m "feat(bacco): handlers puros de empresas com audit e cerca do número principal"
```

---

### Task 3: Rotas REST `/api/v1/companies`

**Files:**
- Create: `app/api/v1/companies/route.ts` (GET, POST)
- Create: `app/api/v1/companies/[id]/route.ts` (GET, PATCH, DELETE)
- Create: `app/api/v1/companies/[id]/contacts/route.ts` (POST vincula, DELETE desvincula)
- Test: `app/api/v1/companies/route.test.ts`

**Interfaces:**
- Consumes: handlers da Task 2; `requireRole` (`@/lib/auth/require-role`), `requireSupportWrite` (`@/lib/impersonate/support`), `ok`/`fail`/`noContent` (`@/lib/api/wrappers`), `validateRequest` (`@/lib/schemas`), `createClient` (`@/lib/supabase/server`).
- Produces: contratos JSON `{ data: Company[] , meta: { cursor, has_more } }`, `{ data: Company & { contacts } }`.

Papéis: ler e vincular = `agent`; apagar = `manager` (spec §5.2a). Sem Bearer nesta v1 (só cookie); se alguém pedir Bearer, entra em `lib/auth/public-paths.ts` junto.

- [ ] **Step 1: Teste da rota (falhando)**

`app/api/v1/companies/route.test.ts`, no molde de `app/api/v1/contacts/route.test.ts`:

```ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import { NextRequest } from "next/server";

vi.mock("@/lib/auth/require-role", () => ({ requireRole: vi.fn() }));
vi.mock("@/lib/impersonate/support", () => ({ requireSupportWrite: vi.fn(async () => null) }));
vi.mock("@/lib/supabase/server", () => ({ createClient: vi.fn(async () => ({})) }));
vi.mock("@/lib/companies/handler", () => ({
  listCompaniesHandler: vi.fn(async () => ({ companies: [], cursor: null, has_more: false })),
  createCompanyHandler: vi.fn(async (_sb, _ctx, input) => ({ id: "c1", ...input })),
}));
import { requireRole } from "@/lib/auth/require-role";
import { createCompanyHandler } from "@/lib/companies/handler";

const ORG_ID = "11111111-1111-4111-8111-111111111111";
function sessao(role: "viewer" | "agent" | "manager") {
  vi.mocked(requireRole).mockImplementation(async (min) => {
    const rank = { viewer: 1, agent: 2, ai_operator: 3, manager: 4, admin: 5 } as const;
    if (rank[role] < rank[min]) {
      const { NextResponse } = await import("next/server");
      return { ok: false, response: NextResponse.json({ error: { code: "forbidden", message: "x" } }, { status: 403 }) } as never;
    }
    return { ok: true, user: { id: "u1", idioma: "pt-BR" }, org: { orgId: ORG_ID, name: "Org", role } } as never;
  });
}

describe("GET /api/v1/companies", () => {
  beforeEach(() => vi.clearAllMocks());
  it("viewer lê a lista", async () => {
    sessao("viewer");
    const { GET } = await import("./route");
    const res = await GET(new NextRequest("http://x/api/v1/companies?limit=10"));
    expect(res.status).toBe(200);
    expect(await res.json()).toMatchObject({ data: [], meta: { has_more: false } });
  });
  it("limit fora da faixa é 422", async () => {
    sessao("viewer");
    const { GET } = await import("./route");
    const res = await GET(new NextRequest("http://x/api/v1/companies?limit=500"));
    expect(res.status).toBe(422);
  });
});

describe("POST /api/v1/companies", () => {
  beforeEach(() => vi.clearAllMocks());
  it("viewer recebe 403 e o handler não roda", async () => {
    sessao("viewer");
    const { POST } = await import("./route");
    const res = await POST(new NextRequest("http://x/api/v1/companies", { method: "POST", body: JSON.stringify({ name: "A" }), headers: { "content-type": "application/json" } }));
    expect(res.status).toBe(403);
    expect(createCompanyHandler).not.toHaveBeenCalled();
  });
  it("agent cria; organization_id do body é ignorado", async () => {
    sessao("agent");
    const { POST } = await import("./route");
    const res = await POST(new NextRequest("http://x/api/v1/companies", { method: "POST", body: JSON.stringify({ name: "A", organization_id: "zzz" }), headers: { "content-type": "application/json" } }));
    expect(res.status).toBe(201);
    const ctx = vi.mocked(createCompanyHandler).mock.calls[0]![1];
    expect(ctx.organization_id).toBe(ORG_ID);
    const input = vi.mocked(createCompanyHandler).mock.calls[0]![2] as Record<string, unknown>;
    expect(input.organization_id).toBeUndefined();
  });
});
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
pnpm vitest run app/api/v1/companies/route.test.ts
```

Esperado: FAIL — módulo `./route` inexistente.

- [ ] **Step 3: Implementar as rotas**

`app/api/v1/companies/route.ts`:

```ts
import { requireSupportWrite } from "@/lib/impersonate/support";
/**
 * GET/POST /api/v1/companies — empresas do tenant (spec §5.2a).
 * Cookie session apenas. viewer lê; agent+ escreve.
 */
import { randomUUID } from "node:crypto";
import type { NextRequest } from "next/server";
import { ok, fail } from "@/lib/api/wrappers";
import { ApiError } from "@/lib/api/types";
import { requireRole } from "@/lib/auth/require-role";
import { createClient } from "@/lib/supabase/server";
import { companyCreateSchema, companyListQuerySchema, validateRequest, type CompanyCreate } from "@/lib/schemas";
import { createCompanyHandler, listCompaniesHandler } from "@/lib/companies/handler";

export const dynamic = "force-dynamic";

export async function GET(req: NextRequest): Promise<Response> {
  const requestId = randomUUID();
  const authz = await requireRole("viewer", { requestId, resource: "companies" });
  if (!authz.ok) return authz.response;
  const sp = req.nextUrl.searchParams;
  const parsed = companyListQuerySchema.safeParse({
    search: sp.get("search") ?? undefined,
    limit: sp.get("limit") ?? undefined,
    cursor: sp.get("cursor") ?? undefined,
  });
  if (!parsed.success) {
    return fail("validation_failed", "Query inválida.", 422, {
      details: parsed.error.flatten().fieldErrors as Record<string, unknown>,
      requestId,
    });
  }
  const supabase = await createClient();
  try {
    const r = await listCompaniesHandler(
      supabase,
      { organization_id: authz.org.orgId, actor: { type: "user", id: authz.user.id }, requestId, idioma: authz.user.idioma },
      parsed.data,
    );
    return ok(r.companies, { requestId, meta: { cursor: r.cursor, has_more: r.has_more } });
  } catch (err) {
    if (err instanceof ApiError) return fail(err.code, err.message, err.status, { details: err.details as Record<string, unknown> | undefined, requestId });
    throw err;
  }
}

export async function POST(req: NextRequest): Promise<Response> {
  const supportDenied = await requireSupportWrite();
  if (supportDenied) return supportDenied;
  const requestId = randomUUID();
  const authz = await requireRole("agent", { requestId, resource: "companies" });
  if (!authz.ok) return authz.response;
  let input: CompanyCreate;
  try {
    input = (await validateRequest(companyCreateSchema, req)) as CompanyCreate;
  } catch (err) {
    if (err instanceof ApiError) return fail(err.code, err.message, err.status, { details: err.details as Record<string, unknown> | undefined, requestId });
    throw err;
  }
  const supabase = await createClient();
  try {
    const empresa = await createCompanyHandler(
      supabase,
      { organization_id: authz.org.orgId, actor: { type: "user", id: authz.user.id }, requestId, idioma: authz.user.idioma },
      input,
    );
    return ok(empresa, { status: 201, requestId });
  } catch (err) {
    if (err instanceof ApiError) return fail(err.code, err.message, err.status, { details: err.details as Record<string, unknown> | undefined, requestId });
    throw err;
  }
}
```

`app/api/v1/companies/[id]/route.ts` — mesmo esqueleto: `GET` (`viewer`, `getCompanyHandler`), `PATCH` (`agent`, `validateRequest(companyPatchSchema, req)`, `patchCompanyHandler`), `DELETE` (`manager`, `deleteCompanyHandler`, `noContent()`), com `ctx: { params: Promise<{ id: string }> }` e `const { id } = await ctx.params;`.

`app/api/v1/companies/[id]/contacts/route.ts` — `POST` (`agent`, `validateRequest(companyLinkContactSchema, req)`, `linkContactHandler`, `ok({ linked: true }, { requestId })`) e `DELETE` (`agent`, mesmo body, `unlinkContactHandler`, `noContent()`).

Conferir a forma exata de `HandlerCtx` em `lib/api/handlers/types.ts` (campos `organization_id`, `actor`, `requestId`, `idioma`) — copiar do uso em `app/api/v1/contacts/route.ts`.

- [ ] **Step 4: Testes das rotas aninhadas** (sem eles a task fica verde com `[id]` ausente)

`app/api/v1/companies/[id]/route.test.ts` e `app/api/v1/companies/[id]/contacts/route.test.ts`, com o mesmo `sessao(role)` do Step 1 e os handlers mockados:

- `GET /[id]` com `viewer` → 200 e `getCompanyHandler` chamado com `{ companyId: id }` e `organization_id` do ctx.
- `PATCH /[id]` com `viewer` → 403, handler não chamado; com `agent` e body `{ billing_contact_id }` → 200.
- `DELETE /[id]` com `agent` → 403; com `manager` → 204.
- `POST /[id]/contacts` com `agent` e `{ contact_id }` → 200 `{ linked: true }`; body sem `contact_id` → 422.
- `DELETE /[id]/contacts` com `agent` → 204 e `unlinkContactHandler` chamado.

```bash
pnpm vitest run app/api/v1/companies; pnpm typecheck
```

- [ ] **Step 5: Commit**

```bash
git add app/api/v1/companies
git commit -m "feat(bacco): rotas REST de empresas (listar, criar, editar, apagar, vincular contato)"
```

---

### Task 4: Contato carrega `company_id` e `company` (aditivo)

**Files:**
- Modify: `app/api/v1/contacts/_handler.ts:34` (`SELECT_COLS`), `patchContactHandler` (aceita `company_id`), `getContactHandler` (anexa `company`)
- Modify: `lib/types/contacts.ts` (`company_id`, `company?`)
- Test: `app/api/v1/contacts/_handler.company.test.ts`

**Interfaces:**
- Produces: `Contact.company_id: string | null`; `Contact.company?: { id: string; name: string; is_billing_contact: boolean } | null` (só no GET de detalhe).

- [ ] **Step 1: Teste (falhando)**

```ts
import { describe, it, expect, vi } from "vitest";
vi.mock("@/lib/audit", () => ({ audit: vi.fn(async () => undefined) }));
vi.mock("@/lib/supabase/admin", () => ({ createAdminClient: vi.fn() }));

const ORG = "11111111-1111-4111-8111-111111111111";
const ctx = { organization_id: ORG, actor: { type: "user" as const, id: "u1" }, requestId: "r1", idioma: "pt-BR" };

it("getContactHandler anexa company quando company_id existe, e null quando não", async () => {
  const { getContactHandler } = await import("./_handler");
  const respostas = [
    { data: { id: "ct1", organization_id: ORG, company_id: "c1", tags: [], custom_fields: {} }, error: null },
    { data: { id: "c1", name: "Adega", billing_contact_id: "ct1" }, error: null },
  ];
  const b: Record<string, unknown> = {};
  for (const m of ["from","select","eq","is","maybeSingle","order","limit"]) {
    b[m] = vi.fn(() => (m === "maybeSingle" ? Promise.resolve(respostas.shift()) : b));
  }
  const out = await getContactHandler(b as never, ctx, { contactId: "ct1" } as never);
  // GetContactResult estende Contact (_handler.ts:272): o campo é direto em `out`.
  expect(out.company).toEqual({ id: "c1", name: "Adega", is_billing_contact: true });
});

it("patchContactHandler grava company_id (a whitelist do patch precisa da linha nova)", async () => {
  const { patchContactHandler } = await import("./_handler");
  const updates: unknown[] = [];
  const b: Record<string, unknown> = {};
  for (const m of ["from","select","eq","is","maybeSingle","order","limit","update","single"]) {
    b[m] = vi.fn((...a: unknown[]) => {
      if (m === "update") updates.push(a[0]);
      return m === "maybeSingle" || m === "single"
        ? Promise.resolve({ data: { id: "ct1", organization_id: ORG, company_id: null, tags: [], custom_fields: {}, consent: {} }, error: null })
        : b;
    });
  }
  await patchContactHandler(b as never, ctx, { contactId: "ct1", patch: { company_id: "c1" } } as never);
  expect(updates[0]).toMatchObject({ company_id: "c1" });
});
```

- [ ] **Step 2: Rodar e ver falhar**

```bash
pnpm vitest run app/api/v1/contacts/_handler.company.test.ts
```

- [ ] **Step 3: Implementar**

Em `SELECT_COLS` acrescentar `, company_id`. Em `lib/types/contacts.ts`:

```ts
  company_id: string | null;
  /** Derivado no GET de detalhe (não é coluna). Ausente quando o contato não tem empresa. */
  company?: { id: string; name: string; is_billing_contact: boolean } | null;
```

Em `getContactHandler`, depois de `const contact = data as Contact;`:

```ts
  if (contact.company_id) {
    const { data: emp } = await supabase
      .from("crm_companies")
      .select("id, name, billing_contact_id")
      .eq("organization_id", ctx.organization_id)
      .eq("id", contact.company_id)
      .maybeSingle();
    contact.company = emp
      ? { id: emp.id, name: emp.name, is_billing_contact: emp.billing_contact_id === contact.id }
      : null;
  }
```

`patchContactHandler` e `createContactHandler` montam o patch/insert por **whitelist explícita** (`_handler.ts:522-545` e `:376-389`): sem uma linha nova em cada, `company_id` passa no Zod e é descartado em silêncio. Acrescentar, nos dois:

```ts
  if (input.company_id !== undefined) patch.company_id = input.company_id;   // no patch
  if (input.company_id !== undefined) insertRow.company_id = input.company_id; // no insert
```

No `select` do estado anterior que o PATCH faz para o audit (`_handler.ts:493`), incluir `company_id`, para o `from/to` da mutação registrar a troca de empresa. Hoje não há mapeamento de `23503` (`:591` só devolve `internal_error`): no tratamento de erro do update e do insert, acrescentar

```ts
  if (error.code === "23503" && error.message.includes("company_id")) {
    throw new ApiError(422, "validation_failed", { company_id: ["Empresa não encontrada."] }, ctx.requestId,
      traduzir("Empresa não encontrada.", ctx.idioma ?? "pt-BR"));
  }
```

`components/contacts/NewContactDialog.criado.test.tsx:15-40` usa uma fixture `satisfies Contact` — ganha `company_id: null`, senão o typecheck quebra.

- [ ] **Step 4: Rodar e ver passar; suíte de contatos inteira**

```bash
pnpm vitest run app/api/v1/contacts
```

Esperado: tudo verde, inclusive `route.test.ts` sem edição.

- [ ] **Step 5: Commit**

```bash
git add app/api/v1/contacts/_handler.ts app/api/v1/contacts/_handler.company.test.ts lib/types/contacts.ts components/contacts/NewContactDialog.criado.test.tsx
git commit -m "feat(bacco): contato expõe company_id e a empresa no detalhe (campo opcional)"
```

---

### Task 5: Hooks react-query de empresas

**Files:**
- Create: `hooks/companies/useCompanyList.ts`, `useCompany.ts`, `useCreateCompany.ts`, `useUpdateCompany.ts`, `useDeleteCompany.ts`, `useLinkCompanyContact.ts`

**Interfaces:**
- Consumes: `apiClient` (`@/lib/api/client`), `showApiError` (`@/components/feedback/ApiErrorToast`), tipos da Task 2.
- Produces: `useCompanyList(filters: { search?: string; limit?: number })` (infinite), `useCompany(id)`, `useCreateCompany()`, `useUpdateCompany(id)`, `useDeleteCompany()`, `useLinkCompanyContact(companyId)` com `{ link(contactId), unlink(contactId) }`.

- [ ] **Step 1: Escrever os hooks** (copiar de `hooks/contacts/useContactList.ts`, `useContact.ts`, `useUpdateContact.ts`; chaves de cache `["companies", filters]`, `["company", id]`; toda mutação invalida `["companies"]`, `["company", id]` e `["contact"]`/`["contacts"]` quando muda vínculo).

`hooks/companies/useLinkCompanyContact.ts`:

```ts
"use client";
import { useMutation, useQueryClient } from "@tanstack/react-query";
import { apiClient } from "@/lib/api/client";
import { showApiError } from "@/components/feedback/ApiErrorToast";

export function useLinkCompanyContact(companyId: string) {
  const qc = useQueryClient();
  const invalida = () => {
    qc.invalidateQueries({ queryKey: ["company", companyId] });
    qc.invalidateQueries({ queryKey: ["companies"] });
    qc.invalidateQueries({ queryKey: ["contacts"] });
    qc.invalidateQueries({ queryKey: ["contact"] });
  };
  const link = useMutation({
    mutationFn: (contactId: string) =>
      apiClient.post(`/api/v1/companies/${companyId}/contacts`, { contact_id: contactId }),
    onError: showApiError,
    onSuccess: invalida,
  });
  const unlink = useMutation({
    mutationFn: (contactId: string) =>
      apiClient.delete(`/api/v1/companies/${companyId}/contacts`, { contact_id: contactId }),
    onError: showApiError,
    onSuccess: invalida,
  });
  return { link, unlink };
}
```

`apiClient.delete(path, body?)` aceita body (`lib/api/client.ts:402`).

- [ ] **Step 2: `pnpm typecheck` verde. Commit**

```bash
git add hooks/companies
git commit -m "feat(bacco): hooks de empresas"
```

---

### Task 6: Telas `/app/companies` e `/app/companies/[id]` + porta na navegação

**Files:**
- Create: `app/app/companies/page.tsx`, `app/app/companies/_client.tsx`
- Create: `app/app/companies/[id]/page.tsx`, `app/app/companies/[id]/_client.tsx`
- Create: `components/companies/CompaniesTable.tsx`, `NewCompanyDialog.tsx`, `EditCompanyDialog.tsx`, `CompanyContactsCard.tsx`, `LinkContactDialog.tsx`
- Modify: `lib/navigation/catalogo.ts:175` (entrada nova logo após Contatos)
- Test: `tests/unit/navegacao-completude.test.ts` (já existe; só precisa passar), `components/companies/CompanyContactsCard.test.tsx`

**Interfaces:**
- Consumes: hooks da Task 5; `useT`; `ContactsTable` não é reusado (colunas diferentes); `useContactList` para a busca do `LinkContactDialog`.

- [ ] **Step 1: Porta na navegação**

Em `lib/navigation/catalogo.ts`, logo após a entrada `/app/contacts`:

```ts
  {
    href: "/app/companies",
    label: "Empresas",
    description: "Clientes pessoa jurídica e os contatos de cada um.",
    icon: "Buildings",
    group: "crm",
    section: "O dia a dia da venda",
    sidebar: true,
  },
```

Os ícones são **Phosphor**, não lucide, e o mapa é fechado sem fallback (`lib/navigation/registry.ts:53-96`, `lib/ui/icons.ts`): `Buildings` já existe nos dois; `Building2` quebraria a tela no browser com a suíte verde.

- [ ] **Step 2: Página de lista**

`app/app/companies/page.tsx`:

```tsx
import { CompaniesListClient } from "./_client";
export const dynamic = "force-dynamic";
export const metadata = { title: "Empresas" };
export default function CompaniesPage() {
  return <CompaniesListClient />;
}
```

`app/app/companies/_client.tsx` — copiar a estrutura de `app/app/contacts/_client.tsx` (busca com debounce 250 ms, `useCompanyList`, `Skeleton`, `Card` de erro, botão "Nova empresa" abrindo `NewCompanyDialog`). Tabela `CompaniesTable` com colunas: Nome (link para `/app/companies/[id]`), Nome fantasia, CNPJ (formatado `00.000.000/0000-00`), Contatos (`contacts_count`). A coluna "vínculo Asaas" que a spec cita entra no plano Asaas, junto com o cartão. "Atividades recentes dos contatos" no detalhe **não** entra na v1 (a timeline é por contato; spec corrigida). Estado vazio: `EmptyState` do repo (`@/components/empty`) com texto "Nenhuma empresa ainda. Cadastre a primeira para agrupar os contatos de um mesmo cliente."

`NewCompanyDialog`: `useForm<{ name; trade_name; cnpj }>`, `companyCreateSchema.safeParse` antes de enviar, `useCreateCompany`, `toast.success(t("Empresa criada"))`, navega para o detalhe.

- [ ] **Step 3: Página de detalhe**

`app/app/companies/[id]/page.tsx` — server component como `contacts/[id]/page.tsx`: `requireAuth()`, `resolveActiveOrg`, checagem estreita de existência (`select id` com `organization_id`), `notFound()`, renderiza `<CompanyDetailClient companyId={id} />`.

`app/app/companies/[id]/_client.tsx` — `useCompany(id)`; três cartões:

1. **Dados** (nome, fantasia, CNPJ, observações) com botão Editar → `EditCompanyDialog` (`companyPatchSchema`, `useUpdateCompany`).
2. **Contatos** → `CompanyContactsCard` (abaixo).
3. **Asaas** — **não** entra neste plano. O plano Asaas adiciona o cartão aqui, condicionado ao módulo ativo.

`components/companies/CompanyContactsCard.tsx`:

```tsx
"use client";
import Link from "next/link";
import { useState } from "react";
import { useT } from "@/hooks/i18n/useT";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";
import { phoneForDisplay } from "@/lib/channels/phone-variants";
import { useLinkCompanyContact } from "@/hooks/companies/useLinkCompanyContact";
import { useUpdateCompany } from "@/hooks/companies/useUpdateCompany";
import { LinkContactDialog } from "@/components/companies/LinkContactDialog";
import type { Company, CompanyContact } from "@/lib/types/companies";

interface Props {
  company: Company;
  contacts: CompanyContact[];
  canWrite: boolean;
}

export function CompanyContactsCard({ company, contacts, canWrite }: Props) {
  const t = useT();
  const [open, setOpen] = useState(false);
  const { unlink } = useLinkCompanyContact(company.id);
  const update = useUpdateCompany(company.id);

  return (
    <Card className="space-y-3 p-4">
      <div className="flex items-center justify-between">
        <h2 className="text-sm font-semibold">{t("Contatos da empresa")}</h2>
        {canWrite && <Button size="sm" onClick={() => setOpen(true)}>{t("Vincular contato")}</Button>}
      </div>
      {contacts.length === 0 ? (
        <p className="text-sm text-muted-foreground">{t("Nenhum contato vinculado. Vincule quem fala por esta empresa.")}</p>
      ) : (
        <ul className="divide-y">
          {contacts.map((c) => (
            <li key={c.id} className="flex items-center justify-between py-2 text-sm">
              <div>
                <Link href={`/app/contacts/${c.id}`} className="font-medium hover:underline">
                  {c.display_name ?? c.name ?? t("Sem nome")}
                </Link>
                <div className="text-muted-foreground">{c.phone_number ? phoneForDisplay(c.phone_number) : "—"}</div>
              </div>
              <div className="flex items-center gap-2">
                {c.is_billing_contact ? (
                  <Badge data-testid="principal">{t("Número principal para cobrança")}</Badge>
                ) : canWrite ? (
                  <Button variant="ghost" size="sm" onClick={() => update.mutate({ billing_contact_id: c.id })}>
                    {t("Tornar principal")}
                  </Button>
                ) : null}
                {canWrite && (
                  <Button variant="ghost" size="sm" onClick={() => unlink.mutate(c.id)}>{t("Desvincular")}</Button>
                )}
              </div>
            </li>
          ))}
        </ul>
      )}
      <LinkContactDialog companyId={company.id} open={open} onOpenChange={setOpen} />
    </Card>
  );
}
```

`LinkContactDialog`: campo de busca → `useContactList({ search })` → lista de resultados (nome + telefone) com botão "Vincular" → `useLinkCompanyContact(companyId).link.mutate(id)`. Contatos que já têm outra empresa aparecem com a etiqueta "já está em outra empresa" e vincular **troca** (o handler só grava `company_id`; o texto avisa).

- [ ] **Step 4: Teste de componente**

`components/companies/CompanyContactsCard.test.tsx` (Testing Library, como `NewContactDialog.criado.test.tsx`): renderiza com 2 contatos, um principal → exatamente um `data-testid="principal"`; o outro tem botão "Tornar principal"; com `canWrite=false` nenhum botão de escrita aparece.

- [ ] **Step 5: Dicionário e gates**

Toda string passada a `t()` nas telas novas entra em `lib/i18n/dicionario.ts` com o espanhol (a chave é o texto em português; ver o cabeçalho do arquivo). Depois:

```bash
pnpm vitest run components/companies tests/unit/navegacao-completude.test.ts tests/unit/branding.test.ts \
  tests/unit/i18n-espanhol-cobre-a-tela.test.ts tests/unit/telas-filtram-a-organizacao-ativa.test.ts \
  tests/unit/telas-sem-dado-de-mentira.test.ts
pnpm typecheck; pnpm lint
```

- [ ] **Step 6: Commit**

```bash
git add app/app/companies components/companies lib/navigation/catalogo.ts lib/i18n/dicionario.ts
git commit -m "feat(bacco): telas de empresas — lista, detalhe, contatos e número principal para cobrança"
```

---

### Task 7: Campo "Empresa" no contato (360 e diálogo de edição)

**Files:**
- Modify: `components/contacts/EditContactDialog.tsx` (campo `company_id`)
- Modify: `app/app/contacts/[id]/_client.tsx` (linha "Empresa" no cartão de dados, link para a empresa, etiqueta de principal)
- Create: `components/companies/CompanyPicker.tsx`
- Test: `components/contacts/EditContactDialog.empresa.test.tsx`

**Regra da spec:** o campo só aparece quando a org tem ≥1 empresa (a lista `useCompanyList({ limit: 1 })` devolve algo) **ou** o módulo Asaas está ativo (o plano Asaas liga esse segundo `ou`; aqui só o primeiro). Sem empresas: o diálogo é idêntico ao de hoje.

- [ ] **Step 1: Teste (falhando)**

```tsx
// renderiza EditContactDialog com useCompanyList mockado vazio → não há label "Empresa";
// com uma empresa → há; escolher e salvar chama useUpdateContact com { company_id }.
```

Escrever os três casos com `vi.mock("@/hooks/companies/useCompanyList")` e `vi.mock("@/hooks/contacts/useUpdateContact")`, no molde de `NewContactDialog.criado.test.tsx`.

- [ ] **Step 2: `CompanyPicker`**

`components/companies/CompanyPicker.tsx`: `Select` (shadcn) alimentado por `useCompanyList({ search, limit: 50 })`, opção "Nenhuma", **item "Nova empresa…"** que abre o `NewCompanyDialog` da Task 6 e, ao criar, seleciona a recém-criada (é o "criar nova inline" da spec), valor `string | null`, `onChange(companyId | null)`. Exporta também `useTemEmpresas()`:

```ts
export function useTemEmpresas(): boolean {
  const q = useCompanyList({ limit: 1 });
  return (q.data?.pages[0]?.data.length ?? 0) > 0;
}
```

- [ ] **Step 3: No `EditContactDialog`**

Acrescentar `company_id?: string | null` em `FormShape`, default `contact.company_id ?? null`; renderizar `<CompanyPicker>` só quando `useTemEmpresas()`; no `onSubmit`, incluir `company_id` no patch **só se mudou** (evita PATCH com campo em todo salvamento).

No `contacts/[id]/_client.tsx` (acrescentar `import Link from "next/link"` — o arquivo importa `Badge` mas não `Link`), no cartão de dados, uma linha:

```tsx
{contact.company && (
  <div className="flex items-center gap-2 text-sm">
    <span className="text-muted-foreground">{t("Empresa")}</span>
    <Link href={`/app/companies/${contact.company.id}`} className="hover:underline">{contact.company.name}</Link>
    {contact.company.is_billing_contact && <Badge>{t("Número principal para cobrança")}</Badge>}
  </div>
)}
```

- [ ] **Step 4: Rodar; commit**

```bash
pnpm vitest run components/contacts tests/unit/i18n-espanhol-cobre-a-tela.test.ts; pnpm typecheck; pnpm lint
git add components/contacts/EditContactDialog.tsx components/contacts/EditContactDialog.empresa.test.tsx components/companies/CompanyPicker.tsx "app/app/contacts/[id]/_client.tsx" lib/i18n/dicionario.ts
git commit -m "feat(bacco): contato mostra e escolhe a empresa quando a organização usa empresas"
```

---

### Task 8: Merge de contatos com empresas diferentes

**Files:**
- Modify: `supabase/migrations/20260917120000_0260_empresas.sql` e o apêndice (mesma migration da Task 1 — nada foi aplicado ainda; é o mesmo PR)
- Test: `tests/invariants/empresas.test.ts` (+1 caso)

**Regra (spec §5.2a, tabela "Merge"): o principal MANDA.** Se o sobrevivente tem `company_id`, fica; se não tem, **continua sem** — não herda do secundário (herdar seria inventar vínculo que ninguém fez). O que o merge precisa tratar é o efeito colateral do repoint automático de FKs: o passo 5 de `fn_mesclar_contatos` varre `pg_constraint` (`baseline.sql:17703-17716`) e reponta **toda** FK para `contacts`, inclusive `crm_companies.billing_contact_id`. Se o secundário era o principal da empresa B e o sobrevivente pertence à empresa A (ou a nenhuma), a empresa B ficaria com um principal fora dela — exatamente o que a cerca do handler proíbe.

- [ ] **Step 1: Teste (falhando)**

```ts
it("mesclar: principal de B era o secundário; sobrevivente é de A → B fica sem principal, A intacta", () => {
  // seed: empresas A e B; S (secundário) company_id=B e B.billing_contact_id=S; P (principal) company_id=A
  // rpc: select public.fn_mesclar_contatos('<ORG_A>', '<P>', array['<S>']::uuid[]);
  // esperado: P.company_id = A (mandou); B.billing_contact_id is null; A.billing_contact_id inalterado
});
```

- [ ] **Step 2: Implementar** — `fn_mesclar_contatos` já é redefinida **três vezes** no baseline (`:17610`, `:18645`, `:19442`; "último bloco vence"). Copiar o corpo da **última** (`:19442`) para a migration 0260 e para o apêndice, e acrescentar, **depois** do passo 5 (repoint de FKs) e antes do passo 6:

```sql
  -- Empresas (0260): o repoint de FKs pode ter posto o sobrevivente como principal
  -- de uma empresa que não é a dele. Principal fora da empresa é proibido pela API;
  -- aqui a regra é soltar, nunca mover o contato de empresa (o principal manda).
  update public.crm_companies e
     set billing_contact_id = null, updated_at = now()
   where e.organization_id = p_organization_id
     and e.billing_contact_id = p_contato_principal
     and e.id is distinct from v_principal.company_id;
```

`v_principal` é o `contacts%rowtype` já declarado na função (`baseline.sql:17621`); nada de `c_principal`. O `revoke`/`grant` da função (`:17883-17884`) se repete após o `create or replace`.

- [ ] **Step 3: `pnpm test:db` na VPS; commit**

```bash
git commit -am "feat(bacco): mesclar contatos não deixa principal de cobrança fora da própria empresa"
```

---

### Task 9: Gate "empresas são aditivas" + mapa de arquitetura + fragmento

**Files:**
- Create: `tests/unit/empresas-sao-aditivas.test.ts`
- Create: `docs/architecture/empresas.architecture.json`
- Create: `.changes/bacco-empresas.md`

- [ ] **Step 1: O gate**

```ts
/**
 * Empresas é aditivo: o CRM não é só do Bacco, e quem não cadastra empresa não
 * pode ver diferença nenhuma. Este teste reprova qualquer ramificação por
 * empresa fora do módulo — importar `@/lib/companies` ou consultar
 * `crm_companies` de leads, conversas, retorno, roteamento ou motor do agente.
 * Passar a coluna `company_id` como qualquer outra (SELECT/PATCH de contatos) é
 * permitido: coluna não é ramificação.
 */
import { describe, it, expect } from "vitest";
import fs from "node:fs";
import path from "node:path";

const RAIZ = path.resolve(__dirname, "../..");
const VIGIADOS = [
  "app/api/v1/contacts", "app/api/v1/leads", "app/api/v1/conversations", "app/api/v1/messages",
  "lib/followup", "lib/routing", "lib/agent-engine", "lib/ai", "lib/leads", "lib/channels",
];
const PERMITIDOS = /^(lib\/companies|lib\/asaas|app\/api\/v1\/companies|app\/app\/companies|components\/companies|hooks\/companies)\//;
/** Só o CRUD de contatos passa a coluna como coluna (SELECT/PATCH); ninguém mais a lê. */
const PASSA_A_COLUNA = /^app\/api\/v1\/contacts\//;

function arquivos(dir: string): string[] {
  const abs = path.join(RAIZ, dir);
  if (!fs.existsSync(abs)) return [];
  const out: string[] = [];
  for (const e of fs.readdirSync(abs, { withFileTypes: true })) {
    const p = path.join(dir, e.name);
    if (e.isDirectory()) out.push(...arquivos(p));
    else if (/\.(ts|tsx)$/.test(e.name) && !/\.test\.tsx?$/.test(e.name)) out.push(p);
  }
  return out;
}

describe("empresas são aditivas", () => {
  const todos = VIGIADOS.flatMap(arquivos).filter((f) => !PERMITIDOS.test(f));
  it("varreu arquivos", () => expect(todos.length).toBeGreaterThan(50));
  for (const f of todos) {
    it(`${f} não ramifica por empresa`, () => {
      const src = fs.readFileSync(path.join(RAIZ, f), "utf8");
      expect(src).not.toMatch(/@\/lib\/companies/);
      expect(src).not.toMatch(/crm_companies/);
      if (!PASSA_A_COLUNA.test(f)) expect(src).not.toMatch(/company_id/);
    });
  }
});
```

Rodar: deve passar já (nada foi tocado nesses diretórios). Sabotar: acrescentar `import "@/lib/companies/handler"` em `lib/followup/enroll.ts`, rodar, ver 1 vermelho, reverter.

- [ ] **Step 2: Mapa de arquitetura**

`docs/architecture/empresas.architecture.json` no formato de `console-de-agencia.architecture.json`: lanes `tela`, `api`, `dados`; nodes `lista` (/app/companies), `detalhe` (/app/companies/[id]), `contato-360`, `rota-companies` (GET/POST /api/v1/companies), `rota-vinculo` (POST/DELETE .../contacts), `crm_companies`, `contacts.company_id`, `crm_lead_links.company`; edges: lista→rota-companies, detalhe→rota-vinculo, rota-vinculo→contacts.company_id, rota-companies→crm_companies, contato-360→contacts.company_id, crm_companies→contacts.company_id ("billing_contact_id aponta o principal"), crm_lead_links.company→crm_companies. `mainPath`: `["lista","detalhe","rota-vinculo","contacts.company_id"]`. Rodar `pnpm vitest run tests/unit/mapas-de-arquitetura.test.ts`.

- [ ] **Step 3: Fragmento**

`.changes/bacco-empresas.md`:

```markdown
---
impacto: capacidade_nova
secao: adicionado
titulo: Empresas — o cliente pessoa jurídica com vários contatos
---
Agora dá para cadastrar uma empresa e vincular a ela os contatos que falam por ela, marcando um como
número principal para aviso de cobrança. Quem não cadastra empresa não vê nada diferente: o campo
"Empresa" só aparece no contato depois da primeira empresa criada. Crédito: @lussandro.
```

`pnpm release:conferir` verde.

- [ ] **Step 4: Commit**

```bash
git add tests/unit/empresas-sao-aditivas.test.ts docs/architecture/empresas.architecture.json .changes/bacco-empresas.md
git commit -m "test(bacco): gate de que empresas são aditivas; mapa e fragmento de release"
```

---

### Task 10: Prova em tela na VPS (E2E) e fechamento

**Files:**
- Create: `tests/e2e/empresas.spec.ts`
- Modify: `.github/workflows/e2e.yml` (`FORA_DO_CI` com motivo: "roda na VPS do Bacco, o CI do fork não sobe Supabase" — ou `SPECS_PARTE_N` se o e2e do fork estiver ligado)
- Evidence: `.superpowers/evidence/empresas/*.png`

- [ ] **Step 1: Spec Playwright** — jornada: login (conta de teste), CRM › Empresas → "Nova empresa" (nome + CNPJ) → detalhe → "Vincular contato" (buscar um contato existente) → vincular um segundo → "Tornar principal" no primeiro → badge visível **uma** vez (`getByTestId("principal")` count 1) → abrir o contato principal → linha "Empresa" com o nome e a badge → editar contato → escolher "Nenhuma" → salvar → detalhe da empresa mostra 1 contato e sem principal. Screenshots em cada marco.

- [ ] **Step 2: Rodar na VPS** (memória `testes-na-vps-nao-local`): banco fresco do `baseline.sql` + `bootstrap-owner.ts`, `next build && next start`, `pnpm exec playwright test tests/e2e/empresas.spec.ts`. Guardar a saída e as imagens em `.superpowers/evidence/empresas/`.

- [ ] **Step 3: Suíte inteira e gates**

```bash
rm -f tsconfig*.tsbuildinfo; pnpm typecheck; echo exit=$?
pnpm lint; echo exit=$?
pnpm test:unit > /tmp/vt.log 2>&1; echo exit=$?; grep -aE "Test Files|Tests |Errors " /tmp/vt.log | tail -3
pnpm build; echo exit=$?
pnpm test:db   # na VPS
```

Registrar no PR o `EXPLAIN` do `GET /api/v1/contacts` (lista sem filtro) antes e depois — o plano de query não pode mudar para org sem empresa.

- [ ] **Step 4: Commit e registro**

```bash
git add tests/e2e/empresas.spec.ts .github/workflows/e2e.yml .superpowers/evidence/empresas
git commit -m "test(bacco): prova em tela de empresas na VPS"
```

Atualizar `docs/testing/user-journey-map.md` com a jornada "Empresas" (P1) e os achados, se houver.

---

## Self-review (feito ao escrever)

- **Cobertura da spec §5.2a:** tabela ✔ (T1), `company_id` ✔ (T1/T4), `billing_contact_id` + cerca "pertence à empresa" ✔ (T2), `target_kind='company'` ✔ (T1), anonimização ✔ (T1), telas ✔ (T6), contato 360 ✔ (T7), API ✔ (T3), merge ✔ (T8), gate aditivo ✔ (T9), mapa ✔ (T9), fragmento ✔ (T9), E2E ✔ (T10). **Fora:** bloco Asaas na tela da empresa e o `ou módulo Asaas ativo` do campo Empresa — pertencem ao plano Asaas.
- **Regra "campo Empresa só com ≥1 empresa":** `useTemEmpresas` (T7).
- **Nomes consistentes:** `crm_companies`, `company_id`, `billing_contact_id`, `is_billing_contact`, `useLinkCompanyContact`, `linkContactHandler/unlinkContactHandler`, ações `company.*` — iguais em todas as tasks.
- **Placeholders:** nenhum "TBD"; onde o nome exato de um símbolo do repo não foi lido (`traduzir`, `HandlerCtx`, `apiClient.delete`), a task diz onde conferir.
- **Números inventados:** `limit` default 25 e máx 100 copiam `contactListQuerySchema`; debounce 250 ms copia a lista de contatos.

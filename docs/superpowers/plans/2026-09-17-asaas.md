# Módulo Asaas — Plano de implementação

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Módulo opcional por organização que liga o CRM à conta Asaas: o agente consulta pendências, manda link/linha digitável/Pix e prorroga boleto vencido dentro da cerca do admin; o webhook do Asaas matricula o número principal num fluxo de retorno quando uma cobrança vence e encerra quando paga.

**Architecture:** Uma linha em `tenant_integrations` (`provider='asaas'`) guarda chave cifrada, config e token do webhook. `lib/asaas/` é o único lugar que fala HTTP com o Asaas. Três ferramentas MCP em `lib/mcp/tools/cobranca.ts` só chegam ao modelo quando a org tem o módulo `healthy` (filtro em `pickToolsFromMcp` por um set carregado nos dois chamadores). Webhook por token de path → `webhook_events_log` (índice único parcial) → `event_log` → consumidor que faz upsert em `asaas_charges` e matricula/cancela no follow-up. Cron diário reconcilia. Quatro avisos novos na Central.

**Tech Stack:** Next.js 16, TypeScript estrito, Supabase (Postgres + RLS + `fn_encrypt_oauth`), Zod, AI SDK tools via MCP, Vitest, Playwright, sandbox Asaas.

**Spec:** `docs/superpowers/specs/2026-09-17-bacco-asaas-design.md` (§3–§13). **Depende do plano `2026-09-17-empresas.md` já entregue** (`crm_companies`, `billing_contact_id`, `contacts.company_id`, `contacts.asaas_customer_id` entra aqui).

## Global Constraints

- **Este plano só começa depois que o plano de Empresas estiver na `bacco`** (hoje o próximo NNNN é 0260, que é dele). **Migration `0261`** (timestamp `20260917130000`), apêndice `-- ---- asaas (migration 0261, bacco) ----`, linha no MANIFEST. Reconferir o número na hora: `ls supabase/migrations/ | grep -oE '_[0-9]{4}_' | tr -d _ | sort -n | tail -1`.
- **`api_audit_log.resource_id` e `agent_inbox_items.ref_id` são `uuid`.** Ids do Asaas (`pay_…`, `cus_…`) vão em `metadata`/`body`, nunca nesses campos; `resourceId` é sempre o uuid do contato, da empresa ou da integração. Para dedup de aviso por customer, `ref_id = uuidv5(NAMESPACE_ASAAS, customerId)` (`node:crypto` `createHash("sha1")` no formato v5) com `ref_kind = null`.
- **Vínculo com o Asaas é sempre escrita declarada.** Nenhuma tool `read` grava: o vínculo por documento é a tool `write` `crm_link_contact_to_billing`; o vínculo pelo operador é rota própria. O gate `tool-read-nao-muta` não enxerga mutação delegada, então a doutrina é cumprida por desenho, não por teste.
- **A ferramenta de reemissão só é montada quando a cerca existe**: o set de capacidades da integração distingue `asaas` (ler) de `asaas:reemitir`. `agent_inbox_items_kind_check` é reconstruído **inteiro** no bloco único do fim do baseline e a migration termina com a lista completa (gate `kind-check-migration-x-baseline`). `webhook_events_log_provider_check` idem, com `meta_cloud` e `zernio` preservados.
- **Nenhuma env nova.** Tudo por org, no banco.
- **`organization_id` de fonte confiável**: `ctx.organizationId` na tool, token de path no webhook, varredura por org no cron. Nunca do body.
- **Documento (CPF/CNPJ) não é persistido pelo módulo**, não vai a log, audit, Central, `raw_body` nem `payload_parsed`. `document`, `cpf`, `cnpj`, `cpfCnpj`, `access_token` entram nas duas listas de redação.
- **Erro do Asaas chega com o texto real** (`errors[].description`) ao agente e à Central. Nunca "falha na operação".
- **Sem número inventado**: `dias` e `max_por_cobranca` não têm default; sem os dois a ferramenta de reemissão não existe.
- **Texto para o dono sem jargão** do gate `catalogo-tools-leigo-friendly` (`mcp, api, endpoint, payload, uuid, schema, query, webhook, enrollment, pointer, handoff, pipeline, stage, lead, tag, backend, json, crud`). "Cobrança", "boleto", "Pix", "pagamento" são permitidos. Toda string de UI entra em `lib/i18n/dicionario.ts` com espanhol.
- **Cron novo** = diretório em `app/api/v1/cron/` + linha em `docker/scheduler/entrypoint.sh` + entrada em `vercel.ts` (gate `cron-routes-scheduled`), e `audit()` só dentro de `if (efeito > 0)` (gate `cron-audita-so-quando-ha-efeito`). A imagem `scheduler` é republicada pelo CI.
- **Medições do sandbox primeiro** (Task 0): o Zod do webhook, o filtro de data do `RECEIVED` e o comportamento do 404 saem da medição, não da memória.
- Commits em conventional commits PT-BR, um por task, rodapé de atribuição da sessão. `pnpm test:db` e E2E rodam **na VPS**.

---

### Task 0: Medir o sandbox (sem código de produto)

**Files:**
- Create: `docs/superpowers/specs/asaas-sandbox-medido-crm.md`

Com a chave de sandbox do `~/bacco-controle` (nunca colada no shell em claro — exportar de arquivo com `set -a; . ~/bacco-controle/.env; set +a` e usar `$ASAAS_API_KEY`), responder as 5 perguntas de §3 da spec, gravando **saída crua** (sem a chave) no arquivo:

- [ ] **Step 1: Cliente + cobrança vencida**

```bash
B=https://api-sandbox.asaas.com/v3
curl -s -H "access_token: $ASAAS_API_KEY" -H 'content-type: application/json' "$B/customers" \
  -d '{"name":"Teste CRM","cpfCnpj":"24971563792","mobilePhone":"51999990001"}' | tee /tmp/cust.json
CUST=$(python3 -c "import json;print(json.load(open('/tmp/cust.json'))['id'])")
curl -s -H "access_token: $ASAAS_API_KEY" -H 'content-type: application/json' "$B/payments" \
  -d "{\"customer\":\"$CUST\",\"billingType\":\"BOLETO\",\"value\":10.0,\"dueDate\":\"2026-09-01\"}" | tee /tmp/pay.json
```

Anotar: o `status` volta `OVERDUE` na hora ou só depois do job noturno do Asaas? (Se só depois, o E2E da Task 11 precisa criar a cobrança um dia antes.)

- [ ] **Step 2: `PUT dueDate` em `OVERDUE`** — `curl -X PUT "$B/payments/$PAY" -d '{"dueDate":"2026-09-30"}'`; anotar o `status` de retorno e o `GET` seguinte.
- [ ] **Step 3: `identificationField` e `pixQrCode`** para BOLETO e para uma cobrança PIX; anotar 200/404 e o corpo.
- [ ] **Step 4: `GET /payments?status=RECEIVED&paymentDate[ge]=2026-09-01`** — existe o filtro? (Pagar no sandbox: `POST /payments/{id}/receiveInCash` com `paymentDate` e `value`.) Comparar com `dateCreated[ge]`.
- [ ] **Step 5: Webhook de teste** — cadastrar no painel do sandbox uma URL de `https://webhook.site` com token `x`, pagar uma cobrança e copiar o envelope `PAYMENT_RECEIVED` e um `PAYMENT_OVERDUE` (se o sandbox emitir; senão registrar que não emite). Depois trocar a URL por uma que responde 404 e ver se `GET /webhooks/{id}` vira `interrupted:true`.
- [ ] **Step 6: Commit** do arquivo de medição. Os passos seguintes citam o número da rodada onde se apoiam.

---

### Task 1: Migration 0261

**Files:**
- Create: `supabase/migrations/20260917130000_0261_asaas.sql`
- Modify: `supabase/baseline.sql` (apêndice + bloco único do `agent_inbox_items_kind_check`)
- Modify: `supabase/migrations/MANIFEST.md`
- Modify: `tests/invariants/rls-completude-varredura.test.ts` (`PROVA_PROPRIA`: `asaas_charges`, `asaas_charge_actions`)
- Modify: `lib/agent-engine/db/repository.ts` (`InboxKind`), `lib/ai/agent-inbox-copy.ts` (`KIND_LABEL`), `lib/ai/inbox-destino.ts` (`POLITICAS_DE_AVISO`)
- Test: `tests/invariants/asaas.test.ts`

**Interfaces:**
- Produces: `tenant_integrations.provider` aceita `'asaas'`; `webhook_events_log.provider` aceita `'asaas'` + índice único `uq_webhook_events_asaas_external`; `contacts.asaas_customer_id`; tabelas `asaas_charges` e `asaas_charge_actions`; kinds `charge_unmatched | charge_overdue_no_flow | charge_reissue_failed | charge_webhook_paused`.

- [ ] **Step 1: Migration**

```sql
-- Módulo Asaas (spec 2026-09-17-bacco-asaas-design). Opt-in por organização.
-- Nada aqui é obrigatório para quem não liga o módulo: colunas nullable, tabelas
-- vazias, CHECKs só ganham valores.

-- 1. providers novos. Os CHECKs são recriados inteiros (add constraint não é idempotente).
alter table public.tenant_integrations drop constraint if exists tenant_integrations_provider_check;
alter table public.tenant_integrations add constraint tenant_integrations_provider_check
  check (provider in ('nuvemshop','vtex','shopify','asaas'));

alter table public.webhook_events_log drop constraint if exists webhook_events_log_provider_check;
alter table public.webhook_events_log add constraint webhook_events_log_provider_check
  check (provider in ('waha','nuvemshop','generic','meta_cloud','zernio','asaas'));

-- 2. idempotência REAL do webhook Asaas. O índice idx_webhook_events_external_id não é
-- único; dois reenvios concorrentes passariam. Parcial por provider: não impõe nada aos outros.
create unique index if not exists uq_webhook_events_asaas_external
  on public.webhook_events_log (organization_id, external_id)
  where provider = 'asaas' and external_id is not null;

-- 3. pessoa física: o vínculo com o cliente do Asaas mora no contato. Empresa: em crm_companies (0260).
alter table public.contacts add column if not exists asaas_customer_id text;
create unique index if not exists uq_contacts_org_asaas_customer
  on public.contacts (organization_id, asaas_customer_id) where asaas_customer_id is not null;

-- 4. estado mínimo por cobrança. Não é espelho: toda decisão lê o Asaas ao vivo.
create table if not exists public.asaas_charges (
  organization_id uuid not null references public.organizations(id) on delete cascade,
  payment_id text not null,
  customer_id text not null,
  holder_kind text check (holder_kind in ('company','contact')),
  holder_id uuid,
  status text not null,
  due_date date not null,
  value_cents integer not null,
  enrollment_id uuid references public.followup_enrollments(id) on delete set null,
  reissue_count integer not null default 0,
  last_event_at timestamptz not null default now(),
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now(),
  primary key (organization_id, payment_id)
);
create index if not exists idx_asaas_charges_org_status on public.asaas_charges (organization_id, status);
create index if not exists idx_asaas_charges_enrollment on public.asaas_charges (enrollment_id) where enrollment_id is not null;
alter table public.asaas_charges enable row level security;
drop policy if exists tenant_isolation_asaas_charges_all on public.asaas_charges;
create policy tenant_isolation_asaas_charges_all on public.asaas_charges
  using (organization_id in (select public.fn_user_org_ids()))
  with check (organization_id in (select public.fn_user_org_ids()));
revoke all on public.asaas_charges from anon;
grant select on public.asaas_charges to authenticated;      -- a tela lê; só o servidor escreve
grant all on public.asaas_charges to service_role;
drop trigger if exists trg_asaas_charges_updated_at on public.asaas_charges;
create trigger trg_asaas_charges_updated_at before update on public.asaas_charges
  for each row execute function public.fn_set_updated_at();

create table if not exists public.asaas_charge_actions (
  id uuid primary key default gen_random_uuid(),
  organization_id uuid not null references public.organizations(id) on delete cascade,
  payment_id text not null,
  action text not null check (action in ('reissue')),
  actor_kind text not null check (actor_kind in ('agent','user','system')),
  actor_id uuid,
  old_due_date date not null,
  new_due_date date not null,
  created_at timestamptz not null default now()
);
create index if not exists idx_asaas_charge_actions_org_payment
  on public.asaas_charge_actions (organization_id, payment_id);
alter table public.asaas_charge_actions enable row level security;
drop policy if exists tenant_isolation_asaas_charge_actions_all on public.asaas_charge_actions;
create policy tenant_isolation_asaas_charge_actions_all on public.asaas_charge_actions
  using (organization_id in (select public.fn_user_org_ids()))
  with check (organization_id in (select public.fn_user_org_ids()));
revoke all on public.asaas_charge_actions from anon;
grant select on public.asaas_charge_actions to authenticated;
grant all on public.asaas_charge_actions to service_role;

-- 5. avisos novos na Central. Lista COMPLETA (gate kind-check-migration-x-baseline exige
-- igualdade com o bloco único do baseline). Copiar a lista vigente da 0242 e acrescentar os 4.
alter table public.agent_inbox_items drop constraint if exists agent_inbox_items_kind_check;
alter table public.agent_inbox_items add constraint agent_inbox_items_kind_check
  check (kind in (
    'appointment_outcome_required','appointment_recovery_review','qr_rescan','routing_unassigned',
    'job_dead','event_dead','budget_exceeded','handoff','promotion_review','judge_unaligned',
    'followup_dead','snooze_expired','next_action_ambiguous','risk_backlog_seeded',
    'reactivation_expired','capabilities_missing','message_send_stuck','midia_nao_lida',
    'channel_template_review','channel_number_alert','promise_unfulfilled',
    'contact_proposal_expired','budget_warning','conhecimento_nao_indexado','voice_call_missed',
    'case_stale',
    -- Asaas (0261)
    'charge_unmatched','charge_overdue_no_flow','charge_reissue_failed','charge_webhook_paused',
    'other'
  ));
-- "existe aviso aberto para esta chave?" — o consumidor pergunta a cada evento. ref_id é uuid:
-- para customer do Asaas o consumidor grava uuidv5(customer_id); para o aviso diário, uuidv5(data).
create index if not exists agent_inbox_items_charge_aberto_idx
  on public.agent_inbox_items (organization_id, kind, ref_id)
  where kind like 'charge_%' and status = 'open';

notify pgrst, 'reload schema';
```

⚠️ Antes de colar a lista de kinds, **conferir a vigente** no bloco único do fim do baseline (`grep -n "agent_inbox_items_kind_check" supabase/baseline.sql | tail -1`) — se outra migration entrou depois da 0242, a lista acima está curta.

- [ ] **Step 2: Apêndice, MANIFEST, bloco único do kind** — o apêndice repete a migration inteira sob o rótulo; o bloco único de `agent_inbox_items_kind_check` que já existe no fim do baseline é **editado** (não duplicado) para incluir os 4 valores. Linha do MANIFEST:

```
| `20260917130000` | `0261_asaas` | Módulo Asaas opt-in por organização: `asaas` em `tenant_integrations`/`webhook_events_log`, índice único parcial de idempotência do webhook, `contacts.asaas_customer_id`, `asaas_charges` (estado por cobrança) e `asaas_charge_actions` (prorrogações), 4 avisos novos na Central. Spec `docs/superpowers/specs/2026-09-17-bacco-asaas-design.md`. |
```

- [ ] **Step 3: TypeScript dos kinds**

`lib/agent-engine/db/repository.ts` — na união `InboxKind`, acrescentar `| 'charge_unmatched' | 'charge_overdue_no_flow' | 'charge_reissue_failed' | 'charge_webhook_paused'`.

`lib/ai/agent-inbox-copy.ts`, em `KIND_LABEL`:

```ts
  charge_unmatched: "Uma cobrança venceu e o cliente não está no CRM",
  charge_overdue_no_flow: "Uma cobrança venceu e ninguém foi avisado",
  charge_reissue_failed: "O Asaas recusou a prorrogação de um boleto",
  charge_webhook_paused: "O Asaas parou de enviar avisos de cobrança",
```

`lib/ai/inbox-destino.ts`, em `POLITICAS_DE_AVISO`:

```ts
  charge_unmatched: { refs: [], orientacao: "Abra a integração Asaas, encontre a cobrança pelo nome do cliente e vincule-a a uma empresa ou a um contato.", geral: { papel: "agent", href: "/app/integrations/asaas", rotulo: "Abrir integração Asaas" } },
  charge_overdue_no_flow: { refs: ["contact"], orientacao: "Escolha o fluxo de retorno para cobrança vencida na integração Asaas, ou marque o número principal da empresa.", geral: { papel: "admin", href: "/app/integrations/asaas", rotulo: "Configurar avisos de cobrança" } },
  charge_reissue_failed: { refs: ["contact"], orientacao: "Leia o motivo do Asaas e resolva no painel dele; o cliente já recebeu a resposta do assistente.", geral: { papel: "agent", href: "/app/integrations/asaas", rotulo: "Abrir integração Asaas" } },
  charge_webhook_paused: { refs: [], orientacao: "O Asaas pausa os avisos depois de falhas seguidas. O sistema religou; confira se a URL do webhook continua cadastrada.", geral: { papel: "admin", href: "/app/integrations/asaas", rotulo: "Conferir webhook" } },
```

(`refs: ["contact"]` exige `ref_kind='contact'` e `ref_id` = id do contato quando há; conferir o tipo `InboxRefKind` e usar exatamente os nomes que ele aceita.)

- [ ] **Step 4: Teste de invariante** (`tests/invariants/asaas.test.ts`) — `sql` e `countAs` vêm de `tests/invariants/gov-helpers.ts` (já exportados); `seedOrg` e `ORG_A/ORG_B/USER_A/USER_B` vêm de `tests/invariants/rls-isolation.helpers.ts`, criado pelo plano de Empresas (Task 1 de lá). Se aquele arquivo não existir, é sinal de que Empresas não entrou — parar.

```ts
describe("asaas (migration 0261)", () => {
  beforeAll(() => { sql(seedOrg(ORG_A, USER_A, "sess-a", "a") + seedOrg(ORG_B, USER_B, "sess-b", "b")); });

  it("webhook_events_log: segundo evento igual do Asaas é recusado pelo índice", () => {
    const ins = `insert into public.webhook_events_log (organization_id, provider, http_method, raw_body, external_id, status, attempts)
                 values ('${ORG_A}','asaas','POST','{}','pay_1:PAYMENT_OVERDUE','received',0);`;
    sql(ins);
    expect(() => sql(ins)).toThrow(/uq_webhook_events_asaas_external/);
    // e para outro provider a duplicata continua passando (o índice é parcial)
    const gen = ins.replace("'asaas'", "'generic'");
    sql(gen); sql(gen);
  });

  it("RLS: org B não lê asaas_charges nem asaas_charge_actions de A", () => {
    sql(`insert into public.asaas_charges (organization_id, payment_id, customer_id, status, due_date, value_cents)
         values ('${ORG_A}','pay_1','cus_1','OVERDUE','2026-09-01',1000) on conflict do nothing;`);
    expect(countAs(USER_B, `select count(*) from public.asaas_charges where organization_id='${ORG_A}';`)).toBe(0);
    expect(countAs(USER_A, `select count(*) from public.asaas_charges where organization_id='${ORG_A}';`)).toBe(1);
  });

  it("authenticated não escreve em asaas_charges — é o GRANT que barra (só select), antes da RLS", () => {
    expect(() => sql(`set role authenticated; select set_config('request.jwt.claims','{"sub":"${USER_A}"}',false);
      insert into public.asaas_charges (organization_id, payment_id, customer_id, status, due_date, value_cents)
      values ('${ORG_A}','pay_2','cus_1','PENDING','2026-10-01',1000);`)).toThrow();
  });

  it("kinds novos entram e o CHECK bate com o TypeScript", () => {
    for (const k of ["charge_unmatched","charge_overdue_no_flow","charge_reissue_failed","charge_webhook_paused"]) {
      sql(`insert into public.agent_inbox_items (organization_id, kind, severity, title, body) values ('${ORG_A}','${k}','warn','t','b');`);
    }
  });
});
```

`PROVA_PROPRIA` ganha `asaas_charges` e `asaas_charge_actions` citando este arquivo.

- [ ] **Step 5: `pnpm test:db` na VPS; regen de tipos; gates unit**

```bash
pnpm vitest run tests/unit/kind-check-migration-x-baseline.test.ts tests/unit/baseline-constraint-reconstruida.test.ts tests/unit/migrations-nao-encolhem-vocabulario.test.ts tests/unit/vocabulario-banco-x-typescript.test.ts
supabase gen types typescript --db-url "$SUPABASE_DB_URL" > lib/database.types.ts
pnpm typecheck
```

- [ ] **Step 6: Commit**

```bash
git add supabase/migrations/20260917130000_0261_asaas.sql supabase/baseline.sql supabase/migrations/MANIFEST.md tests/invariants/asaas.test.ts tests/invariants/rls-completude-varredura.test.ts lib/agent-engine/db/repository.ts lib/ai/agent-inbox-copy.ts lib/ai/inbox-destino.ts lib/database.types.ts
git commit -m "feat(bacco): schema do módulo Asaas — providers, idempotência do webhook, estado por cobrança, avisos (migration 0261)"
```

---

### Task 2: `lib/asaas/` — cliente HTTP, tipos, config da org

**Files:**
- Create: `lib/asaas/tipos.ts`, `lib/asaas/cliente.ts`, `lib/asaas/config.ts`
- Test: `lib/asaas/cliente.test.ts`, `lib/asaas/config.test.ts`

**Interfaces:**
- Produces:
  - `type AsaasAmbiente = "sandbox" | "producao"`; `ASAAS_BASE_URLS`
  - `interface AsaasPayment { id; customer; status; value; dueDate; billingType; invoiceUrl; bankSlipUrl; paymentDate?; description? }`, `interface AsaasCustomer { id; name; cpfCnpj; mobilePhone?; phone? }`, `interface AsaasList<T> { data: T[]; hasMore: boolean; totalCount: number }`, `class AsaasErro extends Error { status; code: string | null; descricao: string; nossoErro: boolean }`
  - `class AsaasCliente { constructor(apiKey: string, ambiente: AsaasAmbiente); request<T>(method, path, opts?: { query?, body? }): Promise<T>; customerPorDocumento(cpfCnpj); customer(id); payments(customerId, status): Promise<AsaasList<AsaasPayment>>; payment(id); identificationField(id); pixQrCode(id); alterarVencimento(id, dueDate); balance(); webhooks(); religarWebhook(id) }`
  - `interface AsaasConfig { ambiente; followup_pointer_id: string | null; reemissao: { dias: number; max_por_cobranca: number } | null; webhook_url_path: string }`, `configSchema` (Zod, valida `store_metadata`)
  - `carregarIntegracaoAsaas(admin, orgId): Promise<{ id; status; config: AsaasConfig; cliente: AsaasCliente; webhookPathToken: string } | null>` — `null` quando não há linha ou `status !== 'healthy'`, ou quando decifrar falha (loga `warn`, nunca lança).
  - `carregarCapacidadesDeIntegracao(admin, orgId): Promise<ReadonlySet<string>>` — `"asaas"` e, com cerca, `"asaas:reemitir"`.

- [ ] **Step 1: Testes (falhando)**

```ts
// lib/asaas/cliente.test.ts
import { describe, it, expect, vi, afterEach } from "vitest";
import { AsaasCliente, AsaasErro } from "./cliente";

const fetchMock = vi.fn();
vi.stubGlobal("fetch", fetchMock);
afterEach(() => fetchMock.mockReset());

function resp(status: number, body: unknown) {
  return Promise.resolve(new Response(JSON.stringify(body), { status, headers: { "content-type": "application/json" } }));
}

describe("AsaasCliente", () => {
  it("monta URL por ambiente e manda a chave no header access_token", async () => {
    fetchMock.mockReturnValueOnce(resp(200, { data: [], hasMore: false, totalCount: 0 }));
    const c = new AsaasCliente("k-123", "sandbox");
    await c.payments("cus_1", "OVERDUE");
    const [url, init] = fetchMock.mock.calls[0]!;
    expect(String(url)).toBe("https://api-sandbox.asaas.com/v3/payments?customer=cus_1&status=OVERDUE&limit=100&offset=0");
    expect((init as RequestInit).headers).toMatchObject({ access_token: "k-123" });
  });

  it("4xx vira AsaasErro com code e descricao do Asaas; invalid_action não é erro nosso", async () => {
    fetchMock.mockReturnValueOnce(resp(400, { errors: [{ code: "invalid_action", description: "Cobrança já recebida." }] }));
    const c = new AsaasCliente("k", "producao");
    await expect(c.alterarVencimento("pay_1", "2026-10-01")).rejects.toMatchObject({ status: 400, code: "invalid_action", descricao: "Cobrança já recebida.", nossoErro: false });
  });

  it("invalid_value é erro nosso", async () => {
    fetchMock.mockReturnValueOnce(resp(400, { errors: [{ code: "invalid_value", description: "dueDate inválida" }] }));
    await expect(new AsaasCliente("k", "producao").alterarVencimento("p", "x")).rejects.toMatchObject({ nossoErro: true });
  });

  it("a chave nunca aparece na mensagem de erro", async () => {
    fetchMock.mockReturnValueOnce(resp(401, { errors: [{ code: "invalid_api_key", description: "Chave inválida" }] }));
    const err = await new AsaasCliente("segredo-xyz", "producao").balance().catch((e) => e as AsaasErro);
    expect(String(err)).not.toContain("segredo-xyz");
  });

  it("timeout de 10s vira AsaasErro status 0", async () => {
    fetchMock.mockImplementationOnce((_u, init) => new Promise((_, rej) => (init as RequestInit).signal!.addEventListener("abort", () => rej(new DOMException("x", "AbortError")))));
    const c = new AsaasCliente("k", "sandbox", { timeoutMs: 5 });
    await expect(c.balance()).rejects.toMatchObject({ status: 0 });
  });
});
```

```ts
// lib/asaas/config.test.ts
import { describe, it, expect } from "vitest";
import { configSchema } from "./config";

describe("configSchema", () => {
  it("sem dias E max_por_cobranca a reemissão é null (nada de default)", () => {
    expect(configSchema.parse({ ambiente: "sandbox" }).reemissao).toBeNull();
    expect(configSchema.parse({ ambiente: "sandbox", reemissao: { dias: 5 } }).reemissao).toBeNull();
    expect(configSchema.parse({ ambiente: "sandbox", reemissao: { dias: 5, max_por_cobranca: 1 } }).reemissao).toEqual({ dias: 5, max_por_cobranca: 1 });
  });
  it("faixas: dias 1..90, max 1..10", () => {
    expect(() => configSchema.parse({ ambiente: "producao", reemissao: { dias: 0, max_por_cobranca: 1 } })).toThrow();
    expect(() => configSchema.parse({ ambiente: "producao", reemissao: { dias: 5, max_por_cobranca: 11 } })).toThrow();
  });
});
```

- [ ] **Step 2: Rodar e ver falhar** — `pnpm vitest run lib/asaas`.

- [ ] **Step 3: Implementar**

`lib/asaas/tipos.ts`:

```ts
export type AsaasAmbiente = "sandbox" | "producao";
export const ASAAS_BASE_URLS: Record<AsaasAmbiente, string> = {
  sandbox: "https://api-sandbox.asaas.com/v3",
  producao: "https://api.asaas.com/v3",
};
export type AsaasStatus = "PENDING" | "OVERDUE" | "RECEIVED" | "CONFIRMED" | "RECEIVED_IN_CASH" | "REFUNDED" | "DELETED" | string;
export interface AsaasPayment {
  id: string; customer: string; status: AsaasStatus; value: number; dueDate: string;
  billingType: "BOLETO" | "PIX" | "CREDIT_CARD" | "UNDEFINED" | string;
  invoiceUrl?: string | null; bankSlipUrl?: string | null; paymentDate?: string | null; description?: string | null;
}
export interface AsaasCustomer { id: string; name: string; cpfCnpj: string; mobilePhone?: string | null; phone?: string | null }
export interface AsaasList<T> { data: T[]; hasMore: boolean; totalCount: number }
export interface AsaasWebhook { id: string; url: string; enabled: boolean; interrupted: boolean }
/** Só o que a spec §3 lista; o resto é ignorado pelo Zod do webhook (Task 6). */
export const EVENTOS_DE_INTERESSE = ["PAYMENT_OVERDUE","PAYMENT_RECEIVED","PAYMENT_CONFIRMED","PAYMENT_DELETED","PAYMENT_REFUNDED","PAYMENT_UPDATED"] as const;
export type EventoAsaas = (typeof EVENTOS_DE_INTERESSE)[number];
```

`lib/asaas/cliente.ts`:

```ts
/**
 * O ÚNICO lugar que fala HTTP com o Asaas. Ferramentas, webhook e cron importam daqui.
 * Erro 4xx vira AsaasErro com `code`/`descricao` do Asaas — o texto vai inteiro ao usuário
 * (Regra nº 1). `nossoErro` separa erro de contrato (invalid_value/invalid_object) de recusa
 * legítima (invalid_action), que é resposta ao cliente e não alerta ao operador.
 */
import { ASAAS_BASE_URLS, type AsaasAmbiente, type AsaasCustomer, type AsaasList, type AsaasPayment, type AsaasWebhook } from "./tipos";

const CODIGOS_NOSSOS = new Set(["invalid_value", "invalid_object", "invalid_api_key"]);

export class AsaasErro extends Error {
  constructor(
    public readonly status: number,
    public readonly code: string | null,
    public readonly descricao: string,
  ) {
    super(`Asaas ${status}${code ? ` ${code}` : ""}: ${descricao}`);
    this.name = "AsaasErro";
  }
  get nossoErro(): boolean {
    return this.status === 0 || this.status >= 500 || (this.code !== null && CODIGOS_NOSSOS.has(this.code));
  }
}

type Metodo = "GET" | "POST" | "PUT" | "DELETE";

export class AsaasCliente {
  private readonly base: string;
  private readonly timeoutMs: number;
  constructor(private readonly apiKey: string, ambiente: AsaasAmbiente, opts: { timeoutMs?: number } = {}) {
    this.base = ASAAS_BASE_URLS[ambiente];
    this.timeoutMs = opts.timeoutMs ?? 10_000;
  }

  async request<T>(method: Metodo, path: string, opts: { query?: Record<string, string | number>; body?: unknown } = {}): Promise<T> {
    const url = new URL(`${this.base}${path}`);
    for (const [k, v] of Object.entries(opts.query ?? {})) url.searchParams.set(k, String(v));
    const ctrl = new AbortController();
    const timer = setTimeout(() => ctrl.abort(), this.timeoutMs);
    let res: Response;
    try {
      res = await fetch(url, {
        method,
        headers: { access_token: this.apiKey, "content-type": "application/json", accept: "application/json" },
        body: opts.body === undefined ? undefined : JSON.stringify(opts.body),
        signal: ctrl.signal,
      });
    } catch (err) {
      throw new AsaasErro(0, null, err instanceof Error && err.name === "AbortError" ? "O Asaas não respondeu em 10 segundos." : "Não foi possível falar com o Asaas.");
    } finally {
      clearTimeout(timer);
    }
    const texto = await res.text();
    let json: unknown = null;
    try { json = texto ? JSON.parse(texto) : null; } catch { json = null; }
    if (!res.ok) {
      const erros = (json as { errors?: Array<{ code?: string; description?: string }> } | null)?.errors ?? [];
      const primeiro = erros[0];
      throw new AsaasErro(res.status, primeiro?.code ?? null, primeiro?.description ?? `HTTP ${res.status}`);
    }
    return json as T;
  }

  customerPorDocumento(cpfCnpj: string) {
    return this.request<AsaasList<AsaasCustomer>>("GET", "/customers", { query: { cpfCnpj: cpfCnpj.replace(/\D/g, "") } });
  }
  customer(id: string) { return this.request<AsaasCustomer>("GET", `/customers/${encodeURIComponent(id)}`); }
  payments(customerId: string, status: string, offset = 0) {
    return this.request<AsaasList<AsaasPayment>>("GET", "/payments", { query: { customer: customerId, status, limit: 100, offset } });
  }
  paymentsPorStatus(status: string, offset = 0, extra: Record<string, string> = {}) {
    return this.request<AsaasList<AsaasPayment>>("GET", "/payments", { query: { status, limit: 100, offset, ...extra } });
  }
  payment(id: string) { return this.request<AsaasPayment>("GET", `/payments/${encodeURIComponent(id)}`); }
  identificationField(id: string) { return this.request<{ identificationField: string; barCode?: string }>("GET", `/payments/${encodeURIComponent(id)}/identificationField`); }
  pixQrCode(id: string) { return this.request<{ payload: string; encodedImage?: string; expirationDate?: string }>("GET", `/payments/${encodeURIComponent(id)}/pixQrCode`); }
  alterarVencimento(id: string, dueDate: string) { return this.request<AsaasPayment>("PUT", `/payments/${encodeURIComponent(id)}`, { body: { dueDate } }); }
  balance() { return this.request<{ balance: number }>("GET", "/finance/balance"); }
  webhooks() { return this.request<AsaasList<AsaasWebhook>>("GET", "/webhooks"); }
  religarWebhook(id: string) { return this.request<AsaasWebhook>("PUT", `/webhooks/${encodeURIComponent(id)}`, { body: { interrupted: false } }); }
}
```

`lib/asaas/config.ts`:

```ts
import { z } from "zod";
import type { SupabaseClient } from "@supabase/supabase-js";
import { decryptWebhookSecret } from "@/lib/webhooks/secrets";
import { logger } from "@/lib/logger";
import { AsaasCliente } from "./cliente";

/** `store_metadata` da linha de tenant_integrations. Sem default para a cerca: os dois ou nenhum. */
export const configSchema = z
  .object({
    ambiente: z.enum(["sandbox", "producao"]),
    followup_pointer_id: z.string().uuid().nullable().optional().default(null),
    reemissao: z.object({ dias: z.number().int().min(1).max(90).optional(), max_por_cobranca: z.number().int().min(1).max(10).optional() }).nullable().optional(),
  })
  .transform((v) => ({
    ambiente: v.ambiente,
    followup_pointer_id: v.followup_pointer_id ?? null,
    reemissao:
      v.reemissao && v.reemissao.dias !== undefined && v.reemissao.max_por_cobranca !== undefined
        ? { dias: v.reemissao.dias, max_por_cobranca: v.reemissao.max_por_cobranca }
        : null,
  }));
export type AsaasConfig = z.infer<typeof configSchema>;

export interface IntegracaoAsaas {
  id: string;
  status: string;
  config: AsaasConfig;
  cliente: AsaasCliente;
  webhookPathToken: string;
}

const COLS = "id, status, store_metadata, oauth_access_token_encrypted, webhook_path_token";

/** null = módulo desligado/inexistente/quebrado. NUNCA lança: quem chama trata null como "sem módulo". */
export async function carregarIntegracaoAsaas(admin: SupabaseClient, orgId: string, opts: { exigirHealthy?: boolean } = {}): Promise<IntegracaoAsaas | null> {
  const { data, error } = await admin.from("tenant_integrations").select(COLS).eq("organization_id", orgId).eq("provider", "asaas").maybeSingle();
  if (error || !data) return null;
  if ((opts.exigirHealthy ?? true) && data.status !== "healthy") return null;
  const parsed = configSchema.safeParse(data.store_metadata ?? {});
  if (!parsed.success) {
    logger.warn("[asaas] store_metadata inválido; módulo tratado como desligado", { org: orgId, issues: parsed.error.issues.map((i) => i.path.join(".")) });
    return null;
  }
  const apiKey = data.oauth_access_token_encrypted ? await decryptWebhookSecret(admin, data.oauth_access_token_encrypted as unknown as string) : null;
  if (!apiKey) {
    logger.warn("[asaas] não consegui decifrar a chave; módulo tratado como desligado", { org: orgId });
    return null;
  }
  return { id: data.id, status: data.status, config: parsed.data, cliente: new AsaasCliente(apiKey, parsed.data.ambiente), webhookPathToken: data.webhook_path_token };
}

/**
 * Capacidades de integração da org, para o filtro de ferramentas do turno. Sem decifrar chave.
 * "asaas" = ler cobranças; "asaas:reemitir" = também prorrogar (só com a cerca preenchida).
 * Set vazio = nada de integração chega ao modelo (direção segura).
 */
export async function carregarCapacidadesDeIntegracao(admin: SupabaseClient, orgId: string): Promise<ReadonlySet<string>> {
  const { data } = await admin.from("tenant_integrations").select("provider, store_metadata").eq("organization_id", orgId).eq("status", "healthy");
  const caps = new Set<string>();
  for (const r of data ?? []) {
    if (r.provider !== "asaas") { caps.add(r.provider as string); continue; }
    const cfg = configSchema.safeParse(r.store_metadata ?? {});
    if (!cfg.success) continue;
    caps.add("asaas");
    if (cfg.data.reemissao) caps.add("asaas:reemitir");
  }
  return caps;
}
```

- [ ] **Step 4: Rodar e ver passar; commit**

```bash
pnpm vitest run lib/asaas; pnpm typecheck
git add lib/asaas
git commit -m "feat(bacco): cliente HTTP do Asaas e leitura da configuração por organização"
```

---

### Task 3: Resolução do titular (empresa > contato) e redação de documento

**Files:**
- Create: `lib/asaas/titular.ts`
- Modify: `lib/mcp/audit.ts:23` (`ARGS_REDACT_KEYS`), `lib/ai/runtime/serialize.ts:8` (`REDACT_KEYS`)
- Test: `lib/asaas/titular.test.ts`, `tests/unit/asaas-redacao-de-documento.test.ts`

**Interfaces:**
- Produces:
  - `type Titular = { kind: "company"; id: string; customerId: string; billingContactId: string | null } | { kind: "contact"; id: string; customerId: string }`
  - `titularDoContato(admin, orgId, contactId): Promise<Titular | { kind: "sem_vinculo"; contactId: string; companyId: string | null }>`
  - `titularPorCustomer(admin, orgId, customerId): Promise<Titular | null>`
  - `vincularContatoPorDocumento(admin, orgId, contactId, document, cliente: AsaasCliente): Promise<{ ok: true; customerId } | { ok: false; motivo: "nao_encontrado" | "telefone_nao_confere" | "contato_de_empresa" }>` — regra §5.3: só pessoa física; confere `mobilePhone`/`phone` do customer contra `contacts.phone_number` via `canonicalPhoneBR`. Chamada **só** pela tool `write` `crm_link_contact_to_billing` (Task 4).
  - `vincularPeloOperador(admin, orgId, alvo: { kind: "contact" | "company"; id }, customerId): Promise<void>` — ato humano (Central/tela): grava `asaas_customer_id` no contato ou na empresa **sem** conferir telefone (a pessoa decidiu), com `23505` mapeado para "este cliente do Asaas já está vinculado a outro cadastro".

- [ ] **Step 1: Testes (falhando)**

```ts
// lib/asaas/titular.test.ts — fakeSb no molde de lib/companies/handler.test.ts
it("contato com empresa vinculada → titular é a empresa, mesmo que o contato tenha asaas_customer_id próprio", ...);
it("contato sem empresa e com asaas_customer_id → titular contato", ...);
it("contato de empresa sem vínculo → sem_vinculo com companyId", ...);
it("titularPorCustomer acha empresa antes de contato", ...);
it("vincularContatoPorDocumento recusa contato de empresa", ...);
it("vincularContatoPorDocumento recusa quando o telefone do customer não bate (+55 51 99999-0001 vs 51 98888-0000)", ...);
it("vincularContatoPorDocumento grava asaas_customer_id quando bate, com update filtrado por organization_id", ...);
```

```ts
// tests/unit/asaas-redacao-de-documento.test.ts
import { redactArgs } from "@/lib/mcp/audit";           // exportar (hoje é privada, audit.ts:32; compara k.toLowerCase())
import { serializeSteps } from "@/lib/ai/runtime/serialize"; // o nome real da função que redige o trace — conferir no arquivo
it("document, cpf, cnpj, cpfCnpj e access_token são redigidos no audit de tool", () => {
  const out = redactArgs({ document: "24971563792", cpfCnpj: "x", cnpj: "y", access_token: "z", contact_id: "ok" }) as Record<string, string>;
  expect(out.document).not.toBe("24971563792");
  expect(out.contact_id).toBe("ok");
});
it("o trace do turno também redige (serialize.ts tem a própria lista)", () => {
  const trace = JSON.stringify(serializeSteps([{ type: "tool-call", toolName: "crm_link_contact_to_billing", args: { document: "24971563792" } }] as never));
  expect(trace).not.toContain("24971563792");
});
```

- [ ] **Step 2: Implementar `titular.ts`**

```ts
import type { SupabaseClient } from "@supabase/supabase-js";
import { canonicalPhoneBR } from "@/lib/channels/phone-variants";
import type { AsaasCliente } from "./cliente";

export type Titular =
  | { kind: "company"; id: string; customerId: string; billingContactId: string | null }
  | { kind: "contact"; id: string; customerId: string };
export type SemVinculo = { kind: "sem_vinculo"; contactId: string; companyId: string | null };

export async function titularDoContato(admin: SupabaseClient, orgId: string, contactId: string): Promise<Titular | SemVinculo> {
  const { data: c } = await admin.from("contacts").select("id, company_id, asaas_customer_id").eq("organization_id", orgId).eq("id", contactId).maybeSingle();
  if (!c) return { kind: "sem_vinculo", contactId, companyId: null };
  if (c.company_id) {
    const { data: e } = await admin.from("crm_companies").select("id, asaas_customer_id, billing_contact_id").eq("organization_id", orgId).eq("id", c.company_id).maybeSingle();
    if (e?.asaas_customer_id) return { kind: "company", id: e.id, customerId: e.asaas_customer_id, billingContactId: e.billing_contact_id };
    return { kind: "sem_vinculo", contactId, companyId: c.company_id };
  }
  if (c.asaas_customer_id) return { kind: "contact", id: c.id, customerId: c.asaas_customer_id };
  return { kind: "sem_vinculo", contactId, companyId: null };
}

export async function titularPorCustomer(admin: SupabaseClient, orgId: string, customerId: string): Promise<Titular | null> {
  const { data: e } = await admin.from("crm_companies").select("id, billing_contact_id").eq("organization_id", orgId).eq("asaas_customer_id", customerId).maybeSingle();
  if (e) return { kind: "company", id: e.id, customerId, billingContactId: e.billing_contact_id };
  const { data: c } = await admin.from("contacts").select("id").eq("organization_id", orgId).eq("asaas_customer_id", customerId).maybeSingle();
  if (c) return { kind: "contact", id: c.id, customerId };
  return null;
}

function digitos(s: string | null | undefined): string { return (s ?? "").replace(/\D/g, ""); }

/** §5.3: pessoa física só. O documento NÃO é persistido; só transita até o Asaas. */
export async function vincularContatoPorDocumento(
  admin: SupabaseClient, orgId: string, contactId: string, document: string, cliente: AsaasCliente,
): Promise<{ ok: true; customerId: string } | { ok: false; motivo: "nao_encontrado" | "telefone_nao_confere" | "contato_de_empresa" }> {
  const { data: c } = await admin.from("contacts").select("id, company_id, phone_number").eq("organization_id", orgId).eq("id", contactId).maybeSingle();
  if (!c) return { ok: false, motivo: "nao_encontrado" };
  if (c.company_id) return { ok: false, motivo: "contato_de_empresa" };
  const lista = await cliente.customerPorDocumento(document);
  const customer = lista.data[0];
  if (!customer) return { ok: false, motivo: "nao_encontrado" };
  const doContato = c.phone_number ? digitos(canonicalPhoneBR(c.phone_number)) : "";
  const doCustomer = [customer.mobilePhone, customer.phone].map((p) => digitos(p)).filter(Boolean).map((p) => (p.startsWith("55") ? p : `55${p}`)).map((p) => digitos(canonicalPhoneBR(`+${p}`)));
  if (!doContato || !doCustomer.includes(doContato)) return { ok: false, motivo: "telefone_nao_confere" };
  const { error } = await admin.from("contacts").update({ asaas_customer_id: customer.id }).eq("organization_id", orgId).eq("id", contactId);
  if (error) throw new Error(`asaas_vinculo_falhou: ${error.message}`);
  return { ok: true, customerId: customer.id };
}
```

- [ ] **Step 3: Redação** — nos dois `Set`s acrescentar `"document", "cnpj", "cpfcnpj", "access_token"` (a comparação é case-insensitive? conferir `redactArgs`; se for exata, acrescentar também `"cpfCnpj"`). Exportar `redactArgs` de `lib/mcp/audit.ts` se for privada.

- [ ] **Step 4: Rodar, commit**

```bash
pnpm vitest run lib/asaas tests/unit/asaas-redacao-de-documento.test.ts
git add lib/asaas/titular.ts lib/asaas/titular.test.ts lib/mcp/audit.ts lib/ai/runtime/serialize.ts tests/unit/asaas-redacao-de-documento.test.ts
git commit -m "feat(bacco): titular da cobrança (empresa antes de contato) e documento redigido do audit"
```

---

### Task 4: Ferramentas do agente + catálogo + filtro por integração

**Files:**
- Create: `lib/mcp/tools/cobranca.ts`, `lib/mcp/tools/catalogo/cobranca.ts`
- Modify: `lib/mcp/tools/index.ts` (import + 3 entradas em `allTools`), `lib/mcp/tools/catalogo/index.ts` (spread), `lib/mcp/tools/catalogo/tipos.ts` (`requerIntegracao?: "asaas"`), `lib/mcp/tools/selecao-por-pacote.ts:80,90` (`entraPorPacote` ganha a entrada, não só o risco), `lib/ai/runtime/tools.ts:36,264` (`integracoesAtivas` em `PickToolsInput` e o skip), `lib/ai/runtime/agent.ts:463` e `lib/agent-engine/edge/crm/mcp-tools.ts:109` (carregam o set), `lib/leads/activity-vocabulary.ts` (tipos + rótulos), `lib/audit/actions.ts`
- Test: `tests/unit/mcp-cobranca-tools.test.ts`, `tests/unit/ponte-nao-monta-integracao-desligada.test.ts`, `tests/unit/mcp-retencao-tools.test.ts` (contagem)

**Interfaces:**
- Produces tools `crm_list_contact_charges` (read), `crm_get_charge_payment_info` (read), `crm_link_contact_to_billing` (write), `crm_reissue_overdue_charge` (write) — todas com `contact_id` obrigatório; `McpToolCatalogEntry.requerIntegracao?: "asaas" | "asaas:reemitir"`; `PickToolsInput.capacidadesDeIntegracao?: ReadonlySet<string>` (opcional; ausente = `new Set()`, fechado — assim os 4 testes que constroem `PickToolsInput` hoje não mudam: `ponte-nao-monta-apenas-humano`, `o-seam-higieniza-o-uuid-do-modelo`, `o-seam-preenche-o-contato-do-turno`, `mcp-retencao-tools`); `ActivityType` += `charge_overdue | charge_paid | charge_deleted | charge_due_changed | charge_reissued | charge_waiting_slot`; `AUDIT_ACTIONS` += `asaas.charge_reissued`, `asaas.contact_linked`, `asaas.company_linked`.

- [ ] **Step 1: Testes (falhando)** — `tests/unit/mcp-cobranca-tools.test.ts` no molde de `mcp-retencao-tools.test.ts` (`fakeSupabase`, `ctxDe`), com `lib/asaas/config` mockado para devolver um `AsaasCliente` cujo `fetch` é stub:

```ts
it("list: contato sem vínculo → { needs_document: true }, sem chamar o Asaas e SEM escrever nada");
it("link: documento confere com o telefone → grava asaas_customer_id (update filtrado por organization_id) e audita asaas.contact_linked com resourceId = contact_id");
it("link: telefone não confere → { refused: 'documento_nao_confere' } sem escrita");
it("link: contato de empresa → { refused: 'contato_de_empresa' }");
it("list: contato de empresa vinculada → lista PENDING+OVERDUE do customer da empresa, com value_cents e days_overdue");
it("payment_info: cobrança de outro customer → { error } sem devolver link (isolamento por contato)");
it("reissue: status RECEIVED ao vivo → recusa como RESPOSTA { refused: 'ja_paga' }, não exceção");
it("reissue: reissue_count >= max → recusa { refused: 'limite', vezes }");
it("reissue: sem cerca na config → tool não é montada (capacidade asaas:reemitir ausente) — coberto no teste da ponte; e se chamada mesmo assim (id gravado + set manipulado), recusa { refused: 'sem_cerca' }");
it("reissue: OK → PUT com dueDate = hoje(fuso da org)+dias, insert em asaas_charge_actions, reissue_count+1, atividade charge_reissued com evidence.trace_ids, audit asaas.charge_reissued com resourceType 'contact', resourceId = contact_id (uuid) e payment_id em metadata");
it("reissue: AsaasErro invalid_action → { error: descricao } sem item na Central; invalid_value → item charge_reissue_failed");
```

`tests/unit/ponte-nao-monta-integracao-desligada.test.ts` (molde: `ponte-nao-monta-apenas-humano.test.ts`):

```ts
// `base` = o `contexto()` de ponte-nao-monta-apenas-humano.test.ts:28-46: { supabase: {} as never, ctx, auth, handoffToolEnabled: false, handoffSignal: { triggered: false } }
const IDS = ["crm_list_contact_charges","crm_get_charge_payment_info","crm_link_contact_to_billing","crm_reissue_overdue_charge","crm_get_contact"];
it("sem capacidadesDeIntegracao (campo ausente) nenhuma tool de cobrança vai ao modelo", () => {
  const tools = pickToolsFromMcp({ ...base, toolIds: IDS });
  expect(Object.keys(tools)).toEqual(["crm_get_contact"]);
});
it("com {asaas} entram as três de leitura/vínculo, mas NÃO a reemissão", () => {
  const tools = pickToolsFromMcp({ ...base, toolIds: IDS, capacidadesDeIntegracao: new Set(["asaas"]) });
  expect(Object.keys(tools).sort()).toEqual(["crm_get_charge_payment_info","crm_get_contact","crm_link_contact_to_billing","crm_list_contact_charges"]);
});
it("com {asaas, asaas:reemitir} as quatro entram", () => {
  const tools = pickToolsFromMcp({ ...base, toolIds: IDS, capacidadesDeIntegracao: new Set(["asaas","asaas:reemitir"]) });
  expect(Object.keys(tools)).toHaveLength(5);
});
```

- [ ] **Step 2: Catálogo** — `lib/mcp/tools/catalogo/tipos.ts` ganha `requerIntegracao?: "asaas" | "asaas:reemitir";` (comentário: "só chega ao modelo quando a organização tem essa capacidade de integração; nunca entra por pacote"). O catálogo **servido** (`lib/mcp/tools/catalogo-servido.ts:35-66`) passa a expor `requer_integracao`, e `tests/unit/catalogo-servido.test.ts:46-60` (que faz `toEqual` das chaves) ganha a chave; `CapacidadeSelecionavel` (`selecao-por-pacote.ts:59-63`) ganha `requerIntegracao?`. `lib/mcp/tools/catalogo/cobranca.ts`:

```ts
import { declararTools } from "./tipos";

export const TOOLS_COBRANCA = declararTools([
  {
    name: "crm_list_contact_charges",
    category: "read",
    rotulo: "Ver as cobranças pendentes do cliente",
    explicacao: "Consulta no Asaas os boletos e Pix em aberto ou vencidos deste cliente (ou da empresa dele), com valor, vencimento e link, para o assistente responder com o dado real e não de memória.",
    oQueToca: "Cobranças no Asaas",
    risco: "seguro",
    pacotes: ["reter", "atender"],
    requerIntegracao: "asaas",
  },
  {
    name: "crm_get_charge_payment_info",
    category: "read",
    rotulo: "Enviar o boleto ou o Pix de uma cobrança",
    explicacao: "Busca no Asaas o link, a linha digitável e o Pix copia-e-cola de uma cobrança específica do cliente, para o assistente mandar na conversa.",
    oQueToca: "Cobranças no Asaas",
    risco: "seguro",
    pacotes: ["reter", "atender"],
    requerIntegracao: "asaas",
  },
  {
    name: "crm_link_contact_to_billing",
    category: "write",
    rotulo: "Reconhecer o cliente no Asaas pelo CPF",
    explicacao: "Quando o cliente informa o CPF na conversa, confere no Asaas se o telefone do cadastro é o mesmo deste atendimento e, se for, liga o contato ao cliente do Asaas. Empresa não passa por aqui: o vínculo dela é feito pela equipe na tela.",
    oQueToca: "Cadastro do cliente",
    risco: "atencao",
    pacotes: ["reter", "atender"],
    requerIntegracao: "asaas",
  },
  {
    name: "crm_reissue_overdue_charge",
    category: "write",
    rotulo: "Prorrogar o vencimento de um boleto vencido",
    explicacao: "Dá uma nova data de vencimento a um boleto já vencido, dentro do limite de dias e de vezes que o administrador definiu na integração Asaas, e devolve o boleto atualizado.",
    oQueToca: "Cobranças no Asaas",
    risco: "atencao",
    pacotes: ["reter"],
    requerIntegracao: "asaas:reemitir",
  },
]);
```

Em `catalogo/index.ts`: `import { TOOLS_COBRANCA } from "./cobranca";` e `...TOOLS_COBRANCA` no array. Em `selecao-por-pacote.ts:80,90`, trocar `entraPorPacote(c.risco)` por `entraPorPacote(c.risco) && !c.requerIntegracao` (o tipo `CapacidadeSelecionavel` precisa expor `requerIntegracao`).

- [ ] **Step 3: Handlers** — `lib/mcp/tools/cobranca.ts`:

```ts
/**
 * Cobranças no Asaas — as três capacidades do módulo (spec §6).
 * Toda query filtra ctx.organizationId; o Asaas não isola por contato, nós isolamos:
 * o handler confere que payment.customer é o customer do titular do contato do turno.
 * Recusa de negócio é RESPOSTA ({ refused }), nunca exceção (padrão do pacote reter).
 */
import { z } from "zod";
import type { McpToolDefinition } from "../types";
import { carregarIntegracaoAsaas } from "@/lib/asaas/config";
import { AsaasErro } from "@/lib/asaas/cliente";
import { titularDoContato, vincularContatoPorDocumento } from "@/lib/asaas/titular";
import { emitAgentActivityForContact } from "@/lib/leads/agent-activity";
import { getRequestPool } from "@/lib/agent-engine/db/request-pool";
import { fusoDaOrganizacao } from "@/lib/agent-engine/agent/fuso-da-org";
import { diaLocalISO } from "@/lib/agenda/fuso";
import { audit } from "@/lib/audit";
import { logger } from "@/lib/logger";
import type { AsaasPayment } from "@/lib/asaas/tipos";

const SEM_MODULO = "A integração com o Asaas não está ativa nesta organização.";

function centavos(v: number): number { return Math.round(v * 100); }
function diasVencidos(dueDate: string, hoje: string): number {
  return Math.max(0, Math.round((Date.parse(hoje) - Date.parse(dueDate)) / 86_400_000));
}
function projetar(p: AsaasPayment, hoje: string) {
  return { payment_id: p.id, status: p.status, billing_type: p.billingType, value_cents: centavos(p.value), due_date: p.dueDate, days_overdue: p.status === "OVERDUE" ? diasVencidos(p.dueDate, hoje) : 0, invoice_url: p.invoiceUrl ?? null };
}
function erroParaOModelo(err: unknown): { error: string } {
  if (err instanceof AsaasErro) return { error: err.descricao };
  return { error: err instanceof Error ? err.message : String(err) };
}

async function hojeDaOrg(orgId: string): Promise<string> {
  const fuso = await fusoDaOrganizacao(getRequestPool(), orgId);
  return diaLocalISO(new Date(), fuso);
}

async function atividade(ctx: { organizationId: string }, contactId: string, type: "charge_reissued", reason: string, payload: Record<string, unknown>, agentId: string | null) {
  try {
    await emitAgentActivityForContact({ pool: getRequestPool(), organizationId: ctx.organizationId, contactId, type, sourceModule: "asaas", sourceId: String(payload.payment_id), evidence: { trace_ids: [String(payload.request_id)] }, agentId, reason, payload });
  } catch (err) {
    logger.warn("[asaas] atividade não gravada", { err: err instanceof Error ? err.message : String(err) });
  }
}

const listShape = {
  contact_id: z.string().uuid().describe("O cliente da conversa."),
};

const linkShape = {
  contact_id: z.string().uuid(),
  document: z.string().min(11).max(18).describe("O CPF que o cliente acabou de informar na conversa."),
};

export const crmLinkContactToBilling: McpToolDefinition<typeof linkShape> = {
  name: "crm_link_contact_to_billing",
  description:
    "Liga este cliente ao cadastro dele no Asaas usando o CPF que ele informou. Só funciona se o telefone do cadastro no Asaas for o deste atendimento. " +
    "Depois de ok, chame crm_list_contact_charges. Se voltar refused, não insista: diga que a equipe vai conferir o cadastro.",
  inputSchema: linkShape,
  category: "write",
  requiresRole: "agent",
  requiresScope: "mcp:write",
  handler: async (input, ctx) => {
    const integ = await carregarIntegracaoAsaas(ctx.supabase, ctx.organizationId);
    if (!integ) return { error: SEM_MODULO };
    try {
      const v = await vincularContatoPorDocumento(ctx.supabase, ctx.organizationId, input.contact_id, input.document, integ.cliente);
      if (!v.ok) {
        if (v.motivo === "telefone_nao_confere") return { refused: "documento_nao_confere", message: "O documento informado não confere com o telefone deste atendimento." };
        if (v.motivo === "contato_de_empresa") return { refused: "contato_de_empresa", message: "Este cliente é de uma empresa; o vínculo é feito pela equipe." };
        return { refused: "nao_encontrado", message: "Não encontrei cadastro no Asaas com esse documento." };
      }
      await audit({ action: "asaas.contact_linked", organizationId: ctx.organizationId, resourceType: "contact", resourceId: input.contact_id, requestId: ctx.requestId, metadata: { customer_id: v.customerId, actor: "agent" } });
      return { ok: true };
    } catch (err) { return erroParaOModelo(err); }
  },
};

export const crmListContactCharges: McpToolDefinition<typeof listShape> = {
  name: "crm_list_contact_charges",
  description:
    "Lista as cobranças em aberto e vencidas deste cliente no Asaas (se ele pertence a uma empresa, as da empresa). " +
    "Se voltar needs_document, peça o CPF ou CNPJ ao cliente e chame de novo com document. " +
    "Se a lista vier vazia, NÃO fale de pendência: o cliente não deve nada agora.",
  inputSchema: listShape,
  category: "read",
  requiresRole: "agent",
  requiresScope: "mcp:read",
  handler: async (input, ctx) => {
    const integ = await carregarIntegracaoAsaas(ctx.supabase, ctx.organizationId);
    if (!integ) return { error: SEM_MODULO };
    const titular = await titularDoContato(ctx.supabase, ctx.organizationId, input.contact_id);
    if (titular.kind === "sem_vinculo") {
      if (titular.companyId) return { needs_link_by_operator: true, message: "A empresa deste cliente ainda não está vinculada ao Asaas. Diga que o cadastro financeiro será conferido pela equipe." };
      return { needs_document: true, message: "Peça o CPF ao cliente e chame crm_link_contact_to_billing." };
    }
    try {
      const hoje = await hojeDaOrg(ctx.organizationId);
      const [pend, venc] = await Promise.all([integ.cliente.payments(titular.customerId, "PENDING"), integ.cliente.payments(titular.customerId, "OVERDUE")]);
      const todas = [...venc.data, ...pend.data].sort((a, b) => b.dueDate.localeCompare(a.dueDate)).map((p) => projetar(p, hoje));
      return { holder: titular.kind, charges: todas, has_more: pend.hasMore || venc.hasMore };
    } catch (err) { return erroParaOModelo(err); }
  },
};

const infoShape = { contact_id: z.string().uuid(), payment_id: z.string().min(1) };

export const crmGetChargePaymentInfo: McpToolDefinition<typeof infoShape> = {
  name: "crm_get_charge_payment_info",
  description: "Devolve link, linha digitável e Pix copia-e-cola de UMA cobrança deste cliente, para você mandar na conversa. Só funciona para cobrança do próprio cliente.",
  inputSchema: infoShape,
  category: "read",
  requiresRole: "agent",
  requiresScope: "mcp:read",
  handler: async (input, ctx) => {
    const integ = await carregarIntegracaoAsaas(ctx.supabase, ctx.organizationId);
    if (!integ) return { error: SEM_MODULO };
    const titular = await titularDoContato(ctx.supabase, ctx.organizationId, input.contact_id);
    if (titular.kind === "sem_vinculo") return { needs_document: true };
    try {
      const p = await integ.cliente.payment(input.payment_id);
      if (p.customer !== titular.customerId) return { error: "Essa cobrança não é deste cliente." };
      const out: Record<string, unknown> = { payment_id: p.id, status: p.status, billing_type: p.billingType, invoice_url: p.invoiceUrl ?? null, bank_slip_url: p.bankSlipUrl ?? null };
      if (p.billingType === "BOLETO") out.identification_field = (await integ.cliente.identificationField(p.id)).identificationField;
      if (p.billingType === "PIX" || p.billingType === "UNDEFINED") {
        const pix = await integ.cliente.pixQrCode(p.id).catch(() => null);   // medição 3 da spec: 404 para tipos sem Pix
        if (pix) { out.pix_copy_paste = pix.payload; out.pix_expires_at = pix.expirationDate ?? null; }
      }
      return out;
    } catch (err) { return erroParaOModelo(err); }
  },
};

const reissueShape = { contact_id: z.string().uuid(), payment_id: z.string().min(1) };

export const crmReissueOverdueCharge: McpToolDefinition<typeof reissueShape> = {
  name: "crm_reissue_overdue_charge",
  description:
    "Prorroga o vencimento de um boleto VENCIDO deste cliente pelo prazo que o administrador definiu e devolve o boleto novo. " +
    "Se voltar refused, explique o motivo ao cliente; se for limite de vezes, ofereça falar com uma pessoa (crm_request_human_handoff).",
  inputSchema: reissueShape,
  category: "write",
  requiresRole: "agent",
  requiresScope: "mcp:write",
  handler: async (input, ctx) => {
    const integ = await carregarIntegracaoAsaas(ctx.supabase, ctx.organizationId);
    if (!integ) return { error: SEM_MODULO };
    if (!integ.config.reemissao) return { refused: "sem_cerca", message: "A prorrogação não está liberada nesta organização." };
    const titular = await titularDoContato(ctx.supabase, ctx.organizationId, input.contact_id);
    if (titular.kind === "sem_vinculo") return { needs_document: true };
    try {
      const p = await integ.cliente.payment(input.payment_id);
      if (p.customer !== titular.customerId) return { error: "Essa cobrança não é deste cliente." };
      if (p.status !== "OVERDUE") return { refused: p.status === "PENDING" ? "ainda_nao_venceu" : "ja_paga", status: p.status };
      const { data: estado } = await ctx.supabase.from("asaas_charges").select("reissue_count").eq("organization_id", ctx.organizationId).eq("payment_id", p.id).maybeSingle();
      const vezes = estado?.reissue_count ?? 0;
      if (vezes >= integ.config.reemissao.max_por_cobranca) return { refused: "limite", vezes, maximo: integ.config.reemissao.max_por_cobranca };
      const hoje = await hojeDaOrg(ctx.organizationId);
      const nova = new Date(Date.parse(hoje) + integ.config.reemissao.dias * 86_400_000).toISOString().slice(0, 10);
      const atualizado = await integ.cliente.alterarVencimento(p.id, nova);
      await ctx.supabase.from("asaas_charges").upsert({ organization_id: ctx.organizationId, payment_id: p.id, customer_id: p.customer, holder_kind: titular.kind, holder_id: titular.id, status: atualizado.status, due_date: nova, value_cents: centavos(p.value), reissue_count: vezes + 1, last_event_at: new Date().toISOString() }, { onConflict: "organization_id,payment_id" });
      await ctx.supabase.from("asaas_charge_actions").insert({ organization_id: ctx.organizationId, payment_id: p.id, action: "reissue", actor_kind: "agent", actor_id: ctx.actor.type === "ai_agent" ? ctx.actor.agent_id ?? null : null, old_due_date: p.dueDate, new_due_date: nova });
      // resource_id é uuid: o contato. O id do Asaas vai em metadata.
      await audit({ action: "asaas.charge_reissued", organizationId: ctx.organizationId, resourceType: "contact", resourceId: input.contact_id, requestId: ctx.requestId, metadata: { payment_id: p.id, old_due_date: p.dueDate, new_due_date: nova, vezes: vezes + 1 } });
      await atividade(ctx, input.contact_id, "charge_reissued", `Boleto prorrogado de ${p.dueDate} para ${nova}`, { payment_id: p.id, request_id: ctx.requestId, new_due_date: nova }, ctx.actor.type === "ai_agent" ? ctx.actor.agent_id ?? null : null);
      const ident = atualizado.billingType === "BOLETO" ? (await integ.cliente.identificationField(p.id).catch(() => null))?.identificationField ?? null : null;
      return { new_due_date: nova, invoice_url: atualizado.invoiceUrl ?? null, bank_slip_url: atualizado.bankSlipUrl ?? null, identification_field: ident };
    } catch (err) {
      if (err instanceof AsaasErro && err.nossoErro) {
        await ctx.supabase.from("agent_inbox_items").insert({ organization_id: ctx.organizationId, kind: "charge_reissue_failed", severity: "warn", title: "O Asaas recusou a prorrogação de um boleto", body: `Cobrança ${input.payment_id}: ${err.descricao}`, ref_kind: "contact", ref_id: input.contact_id });
      }
      return erroParaOModelo(err);
    }
  },
};
```

Conferir a forma de `ctx.actor` (`Actor` em `lib/api/handlers/types.ts`) para `agent_id`.

- [ ] **Step 4: Registro + vocabulários** — `lib/mcp/tools/index.ts`: import das quatro e entradas em `allTools`. `activity-vocabulary.ts`: os seis tipos na união e em `ACTIVITY_LABELS` ("Cobrança vencida", "Cobrança paga", "Cobrança cancelada no Asaas", "Vencimento alterado no Asaas", "Boleto prorrogado", "Cobrança vencida aguardando outro retorno terminar"). `lib/audit/actions.ts`: `asaas.charge_reissued`, `asaas.contact_linked`, `asaas.company_linked`, `asaas.integration_enabled`, `asaas.integration_disabled`, `asaas.integration_config_changed`, `asaas.webhook_token_rotated`, `asaas.webhook_invalid_signature`.

- [ ] **Step 5: Filtro no turno** — `lib/ai/runtime/tools.ts`: `PickToolsInput.capacidadesDeIntegracao?: ReadonlySet<string>` (opcional; ausente = fechado), e em `pickToolsFromMcp`, logo após o skip de `apenasHumano` (`tools.ts:283`; `catalogEntry` já vem de `@/lib/mcp/tools/catalog`, `tools.ts:23`):

```ts
    const req = catalogEntry(def.name)?.requerIntegracao;
    if (req && !(input.capacidadesDeIntegracao ?? new Set()).has(req)) continue;
```

`lib/ai/runtime/agent.ts` (antes do `pickToolsFromMcp`, `:472`): `const capacidadesDeIntegracao = await carregarCapacidadesDeIntegracao(admin, run.organization_id);` e passar no input. `lib/agent-engine/edge/crm/mcp-tools.ts:109` idem com `cfg.supabase` e `ids.organizationId`.

- [ ] **Step 6: Rodar** — `pnpm vitest run tests/unit/mcp-cobranca-tools.test.ts tests/unit/ponte-nao-monta-integracao-desligada.test.ts tests/unit/mcp-retencao-tools.test.ts tests/unit/catalogo-tools-leigo-friendly.test.ts tests/unit/catalogo-servido.test.ts tests/unit/audit-lista-do-painel-e-derivada.test.tsx lib/ai/runtime; pnpm typecheck`. O `mcp-retencao-tools` "seis capacidades" passa a contar as de `reter` sem `requerIntegracao` (ou nove; ler o que o teste afirma e ajustar a frase e o número).

- [ ] **Step 7: Commit**

```bash
git add lib/mcp lib/ai/runtime lib/agent-engine/edge/crm/mcp-tools.ts lib/leads/activity-vocabulary.ts lib/audit/actions.ts tests/unit/mcp-cobranca-tools.test.ts tests/unit/ponte-nao-monta-integracao-desligada.test.ts tests/unit/mcp-retencao-tools.test.ts
git commit -m "feat(bacco): capacidades de cobrança do agente — consultar, enviar boleto/Pix, reconhecer pelo CPF, prorrogar vencido — só com o Asaas ativo"
```

---

### Task 5: Tela `/app/integrations/asaas` + server actions

**Files:**
- Create: `app/app/integrations/asaas/page.tsx`, `app/app/integrations/asaas/_components/{FormularioAsaas,BotoesAsaas,TokenUmaVez}.tsx`
- Create: `app/actions/integrations/asaas.ts` (`salvarConfigAsaas`, `testarConexaoAsaas`, `ativarAsaas`, `desativarAsaas`, `esquecerChaveAsaas`, `girarTokenAsaas`)
- Create: `lib/asaas/validacao-do-fluxo.ts`
- Modify: `lib/navigation/catalogo.ts` (entrada `/app/integrations/asaas`, grupo `organizacao` — o Nuvemshop vive em `canais` sem sidebar, mas Asaas não é canal de conversa; é configuração da organização —, `minRole: "admin"`, `section` obrigatória do grupo), `lib/i18n/dicionario.ts`
- Test: `lib/asaas/validacao-do-fluxo.test.ts`, `app/actions/integrations/asaas.test.ts`

**Interfaces:**
- Produces: `validarFluxoDeCobranca(admin, orgId, pointerId): Promise<{ ok: true; agentId: string; avisoTextoFixo: boolean } | { ok: false; motivo: "inexistente" | "inativo" | "gatilho_errado" | "sem_agente" | "agente_sem_capacidades"; detalhe: string }>` — usada pela action ao salvar e pelo consumidor (Task 7).

- [ ] **Step 1: Teste da validação (falhando)**

```ts
it("pointer inativo → inativo");
it("trigger_config.kind != webhook → gatilho_errado");
it("nenhum agente publicado arma o pointer → sem_agente");
it("agente arma mas tool_ids não tem as 3 → agente_sem_capacidades com a lista do que falta");
it("fluxo com nó action mode=text → ok com avisoTextoFixo=true");
```

- [ ] **Step 2: Implementar `validacao-do-fluxo.ts`** — lê `followup_flow_pointers` (`status`, `trigger_config`, `active_version_id`), `followup_flow_versions.graph` (procura `nodes[].action?.mode === "text"`), `resolveAgentForAutomaticTrigger(createSupabaseFollowupGateDb(admin), orgId, pointerId)`, e `ai_agent_versions.tool_ids` da versão publicada desse agente (`status='published'`). As três constantes de nome vêm de `lib/mcp/tools/catalogo/cobranca.ts` (`TOOLS_COBRANCA.map(t => t.name)`), nunca string solta.

- [ ] **Step 3: Actions** (molde `disconnectNuvemshop.ts`: `loadAuthUser` → `supportWriteError` → `resolveActiveOrg` → `role !== "admin" && !is_platform_admin` → `forbidden`; resultado é união discriminada, nunca throw):

- `salvarConfigAsaas({ apiKey?, ambiente, followup_pointer_id, reemissao })`: `oauth_access_token_encrypted` e `webhook_secret_encrypted` são **`bytea NOT NULL`** (`baseline.sql:1811,1819`), então a **primeira** gravação exige `apiKey` (sem ela: `{ ok:false, error:"chave_obrigatoria" }`) e gera na hora `webhook_path_token` (default do schema) + segredo de 32 bytes cifrado; devolve `{ ok:true, token?: string }` com o segredo em claro **só nessa primeira vez** (a tela mostra URL + token uma vez). Nas seguintes, `apiKey` é opcional (troca de chave). `encryptWebhookSecret(admin, apiKey)` → `oauth_access_token_encrypted`; `configSchema` valida; se `followup_pointer_id`, `validarFluxoDeCobranca` decide (`ok:false` → `{ ok:false, error:"fluxo_invalido", detalhe }` e **não salva**); upsert com `provider:'asaas'`, `status` mantido (ou `'connecting'` na primeira vez), `store_metadata` = config; audit `asaas.integration_config_changed` com `last4` da chave se mudou.
- `testarConexaoAsaas()`: `carregarIntegracaoAsaas(admin, orgId, { exigirHealthy: false })` → `cliente.balance()` → `status='healthy'`, `last_health_check_at`; `AsaasErro` → `status='error'`, `status_reason = err.descricao`. Devolve `{ ok, mensagem }` com o texto real.
- `ativarAsaas()`: testa conexão; `status='healthy'`; audit `asaas.integration_enabled`; dispara `fetch` interno do cron de reconciliação (fire-and-forget, com `INTERNAL_SECRET`); devolve `{ ok:true, webhookUrl }`. O segredo já saiu no primeiro `salvarConfigAsaas`; quem perdeu usa "Gerar novo token".
- `girarTokenAsaas()`: segredo novo, audit `asaas.webhook_token_rotated`, devolve o novo uma vez.
- `desativarAsaas()`: conta matrículas vivas em `asaas_charges` com `enrollment_id` (join em `followup_enrollments.status in ('active','waiting_reply','paused_handoff','paused_manual')`); a UI já mostrou o número e pediu confirmação; cancela cada uma com o guard otimista de `gatilho-caso.ts:400-419`, `cancel_reason:'asaas_disabled'` e `outcome:'exhausted'` (`outcome` é CHECK fechado: `converted|replied|exhausted|opted_out|handoff`, `baseline.sql:7349`); atividade `followup_cancelled` (já existe no vocabulário) com `reason: "Integração Asaas desativada"`. `status='disconnected'`, `status_reason='user_disconnected'`; audit `asaas.integration_disabled` com a contagem.
- `esquecerChaveAsaas()`: `delete` da linha; audit.

- [ ] **Step 4: Página** (server component como `nuvemshop/page.tsx`; **toda** query leva `.eq("organization_id", activeOrg.orgId)` — gate `telas-filtram-a-organizacao-ativa`): estados `sem_linha` (formulário com a chave obrigatória; ao salvar, `TokenUmaVez` mostra URL + token), `configurada` (`status` ≠ healthy: formulário + Testar + Ativar), `ativa` (badge, URL do webhook sempre visível, "Gerar novo token", config editável, "Desativar" com o número de matrículas vivas no texto de confirmação, "Esquecer chave"), `erro` (`status_reason` em destaque). `TokenUmaVez` mostra URL + token com botão copiar e aviso "não será mostrado de novo". `minRole: "admin"` na navegação **e** `activeOrg.role !== "admin"` na página → `notFound()` (o Nuvemshop só esconde botões; aqui a spec exige admin).

Campo "Fluxo de retorno para cobrança vencida": `Select` com os `followup_flow_pointers` ativos da org cujo `trigger_config.kind === 'webhook'`; ao lado, o resultado de `validarFluxoDeCobranca` em texto ("o agente Lina arma este fluxo e tem as capacidades" / "faltam: …" / "este fluxo tem mensagem fixa; recomendamos mensagem do assistente").

- [ ] **Step 5: Dicionário, gates, commit**

```bash
pnpm vitest run lib/asaas app/actions/integrations tests/unit/navegacao-completude.test.ts tests/unit/i18n-espanhol-cobre-a-tela.test.ts tests/unit/telas-filtram-a-organizacao-ativa.test.ts tests/unit/telas-sem-dado-de-mentira.test.ts tests/unit/branding.test.ts
pnpm typecheck; pnpm lint
git add app/app/integrations/asaas app/actions/integrations/asaas.ts app/actions/integrations/asaas.test.ts lib/asaas/validacao-do-fluxo.ts lib/asaas/validacao-do-fluxo.test.ts lib/navigation/catalogo.ts lib/i18n/dicionario.ts
git commit -m "feat(bacco): tela e ações da integração Asaas — chave, ambiente, cerca de prorrogação, fluxo de retorno, webhook"
```

---

### Task 6: Webhook `POST /api/v1/webhooks/asaas/[token]`

**Files:**
- Create: `lib/asaas/webhook.ts` (Zod do envelope, sanitização, mapeamento evento→tipo interno), `app/api/v1/webhooks/asaas/[token]/route.ts`
- Test: `lib/asaas/webhook.test.ts`, `app/api/v1/webhooks/asaas/[token]/route.test.ts`

**Interfaces:**
- Produces: `envelopeSchema` (da medição 5 da Task 0), `sanitizar(raw: string, parsed: unknown): { rawBody: string; payloadParsed: Record<string, unknown> }` (substitui `cpfCnpj`, `creditCard*` por `"[redigido]"`), `tipoInterno(event): "asaas.payment_overdue" | "asaas.payment_received" | "asaas.payment_deleted" | "asaas.payment_updated" | null`.

- [ ] **Step 1: Testes (falhando)**

```ts
// lib/asaas/webhook.test.ts
it("PAYMENT_RECEIVED e PAYMENT_CONFIRMED → asaas.payment_received; PAYMENT_REFUNDED → asaas.payment_deleted; PAYMENT_CREATED → null");
it("sanitizar remove cpfCnpj e creditCard do raw e do parsed");
// route.test.ts (molde: tests de webhooks/in)
it("token desconhecido ou integração não healthy → 200 vazio, sem insert em webhook_events_log");
it("asaas-access-token errado → 401 + audit asaas.webhook_invalid_signature");
it("evento de interesse → insert em webhook_events_log com provider asaas, external_id 'pay:EVENT', headers SEM asaas-access-token, raw_body sem cpfCnpj; rpc emit_event com p_organization_id da integração");
it("insert 23505 (duplicado) → 200 sem emit_event");
it("evento fora da lista → log processed, sem emit_event");
it("erro no emit_event → 200 e status='error' no log");
it("erro genérico no insert do log (não 23505) → 500 e emit_event NÃO chamado");
it("token de mesmo comprimento em caracteres mas bytes diferentes (unicode) → 401, sem lançar");
it("timingSafeEqual é chamado exatamente 1x com token errado do mesmo tamanho (spy em node:crypto, padrão de tests/unit/meta-webhook.test.ts:35) — trocar por === zera a chamada");
```

- [ ] **Step 2: Rota** — esqueleto de `webhooks/in/[token]/route.ts` (`dynamic`, `runtime = "nodejs"`, `checkRateLimit(\`webhook_asaas:${token}\`, 120, 60)`), com as diferenças:

```ts
  const { data: integ } = await admin.from("tenant_integrations")
    .select("id, organization_id, status, webhook_secret_encrypted")
    .eq("provider", "asaas").eq("webhook_path_token", token).maybeSingle();
  if (!integ || integ.status !== "healthy") return new NextResponse(null, { status: 200 }); // §7 passo 1; medição 4 pode trocar por 404

  const rawBody = await req.text();
  const segredo = integ.webhook_secret_encrypted ? await decryptWebhookSecret(admin, integ.webhook_secret_encrypted as unknown as string) : null;
  const recebido = Buffer.from(req.headers.get("asaas-access-token") ?? "", "utf8");
  const esperado = Buffer.from(segredo ?? "", "utf8");
  // comparar tamanho em BYTES (não em caracteres): token multibyte de mesmo length quebraria o timingSafeEqual
  const confere = !!segredo && recebido.length === esperado.length && timingSafeEqual(recebido, esperado);
  if (!confere) {
    await audit({ action: "asaas.webhook_invalid_signature", organizationId: integ.organization_id, resourceType: "tenant_integration", resourceId: integ.id, requestId });
    return fail("unauthenticated", "invalid_token", 401, { requestId });
  }
  let parsed: unknown; try { parsed = JSON.parse(rawBody); } catch { return fail("invalid_request", "invalid_json", 400, { requestId }); }
  const env = envelopeSchema.safeParse(parsed);
  const tipo = env.success ? tipoInterno(env.data.event) : null;
  const { rawBody: rawSan, payloadParsed } = sanitizar(rawBody, parsed);
  const headersJson: Record<string, string> = {};
  req.headers.forEach((v, k) => { const kk = k.toLowerCase(); if (kk.startsWith("authorization") || kk === "cookie" || kk === "asaas-access-token") return; headersJson[k] = v; });
  const externalId = env.success ? `${env.data.payment.id}:${env.data.event}` : null;
  const { data: logRow, error: logErr } = await admin.from("webhook_events_log").insert({
    organization_id: integ.organization_id, provider: "asaas", webhook_path_token: token, http_method: "POST",
    headers: headersJson, raw_body: rawSan, payload_parsed: payloadParsed, signature_header: null, valid_signature: true,
    event_type: env.success ? env.data.event : "unknown", external_id: externalId, status: tipo ? "received" : "processed", attempts: 0,
  }).select("id").single();
  if (logErr?.code === "23505") return ok({ duplicate: true }, { requestId });
  if (logErr || !logRow) {
    // Sem o registro forense não emitimos evento: 500 faz o Asaas tentar de novo (ele só
    // pausa a fila após falhas seguidas; o cron de §9 cobre o resto). Nunca "200 e perdeu".
    logger.error("[asaas.webhook] falha ao gravar webhook_events_log", { org: integ.organization_id, err: logErr?.message });
    return fail("internal_error", "log_failed", 500, { requestId });
  }
  if (!tipo) return ok({ ignored: true }, { requestId });
  const { error: emitErr } = await admin.rpc("emit_event", { p_event_type: tipo, p_entity_kind: "asaas_webhook", p_entity_id: null, p_payload: payloadParsed, p_metadata: { external_id: externalId, log_id: logRow?.id }, p_organization_id: integ.organization_id });
  if (emitErr) { await admin.from("webhook_events_log").update({ status: "error", error_message: emitErr.message }).eq("id", logRow!.id); }
  return ok({ received: true }, { requestId });
```

- [ ] **Step 3: Rodar, commit**

```bash
pnpm vitest run lib/asaas/webhook.test.ts "app/api/v1/webhooks/asaas"
git add lib/asaas/webhook.ts lib/asaas/webhook.test.ts app/api/v1/webhooks/asaas
git commit -m "feat(bacco): webhook do Asaas por token de organização, idempotente e sem documento no log"
```

---

### Task 7: Consumidor do `event_log` (matricula, cancela, avisa)

**Files:**
- Create: `lib/asaas/consumidor.ts` (lógica pura com `ConsumidorDb`), `lib/asaas/consumidor.handler.ts`, `lib/asaas/consumidor.db.ts` (adapter Supabase)
- Modify: `lib/event-log/register-handlers.ts` (registrar perto dos gatilhos de follow-up)
- Test: `lib/asaas/consumidor.test.ts`

**Interfaces:**
- Produces: `asaasCobrancaHandler: EventHandler` (`key: "asaas-cobranca.v1"`, `events: ["asaas.payment_overdue","asaas.payment_received","asaas.payment_deleted","asaas.payment_updated"]`); `interface ConsumidorDb { upsertCharge; carregarCharge; titularPorCustomer; billingContactDaEmpresa; enrollmentViva(enrollmentId): Promise<boolean>; enroll(pointerId, contactId): Promise<EnrollFollowupResult>; cancelaEnrollment(id, reason, outcome: "converted" | "exhausted"): Promise<boolean>; abrirAviso(kind, refKind: "contact" | null, refId: string /* uuid */, title, body): Promise<void> (dedup por (org, kind, ref_id) aberto); atividade(contactId, type, reason, payload); vencidasAoVivo(customerId): Promise<number | "falhou">; validarFluxo(pointerId) }`; `chaveDeAviso(kind, semente: string): string` = uuid v5 determinístico (`node:crypto` sha1 sobre `NAMESPACE_ASAAS + semente`, bits de versão/variante ajustados) — é o que vai em `ref_id` para "1 por customer" (`semente = customerId`) e "1 por dia" (`semente = YYYY-MM-DD`), porque `ref_id` é `uuid` (`baseline.sql:6459`). `resourceId` de audit e `ref_id` nunca recebem `pay_…`/`cus_…`.

- [ ] **Step 1: Testes (falhando)** — `ConsumidorDb` em memória:

```ts
it("overdue: titular empresa com principal → 1 enroll no principal; overdue de novo do mesmo payment → nada (enrollment viva)");
it("overdue: empresa sem principal → aviso charge_overdue_no_flow com nome; zero enroll");
it("overdue: sem titular → aviso charge_unmatched (1 por customer); zero enroll");
it("overdue: config sem followup_pointer_id → charge_overdue_no_flow 1x por dia (ref_id = data)");
it("overdue: validarFluxo falha (agente sem capacidades) → charge_overdue_no_flow com detalhe");
it("overdue: enroll devolve 409 → atividade charge_waiting_slot, enrollment_id null, sem aviso");
it("overdue: enroll lança service_channel_not_found → charge_overdue_no_flow com o texto real");
it("received: enrollment viva e vencidasAoVivo=0 → cancela com cancel_reason charge_settled e outcome 'converted' (CHECK fechado) + atividade charge_paid");
it("received: vencidasAoVivo=2 → mantém");
it("received: vencidasAoVivo='falhou' → cancela mesmo assim e a atividade registra o erro");
it("updated: dueDate diferente do último e sem charge_action com essa data → atividade charge_due_changed; com charge_action igual → nada");
it("evento com organization_id sem integração healthy → skipped");
```

- [ ] **Step 2: Implementar** — `consumidor.ts` recebe `{ db, agora }` e `EventRow`, devolve `{ status: "ok"|"skipped", detail }`. Regras exatamente as de §8 da spec. `consumidor.handler.ts` como `gatilho-caso.handler.ts` (try/catch → `status:"error"` com a mensagem). `consumidor.db.ts` implementa `ConsumidorDb` sobre `createAdminClient()`: `enroll` chama `enrollFollowupFlow(admin, { organizationId, pointerId, contactId, actorUserId: null, requestId: row.id })`; `cancelaEnrollment` copia o guard de `gatilho-caso.ts:400-419` com `cancel_reason` e `outcome` recebidos; `atividade` usa `emitAgentActivityForContact` com `getRequestPool()` (`lib/agent-engine/db/request-pool.ts:14`, singleton lazy por processo sobre `SUPABASE_DB_URL`). **Decisão registrada:** no worker isso é um segundo pool ao lado do de `workers/agent-worker/main.ts:87`; `ensureHandlersRegistered()` não recebe dependências e `gatilho-caso` não emite atividade, então não há precedente a copiar. Um pool pequeno a mais por processo é aceito; injetar pool no registro dos handlers é refatoração fora deste plano (`// ponytail: segundo pool no worker; injetar quando register-handlers aceitar deps`).

Registro em `register-handlers.ts`: `registerHandler(asaasCobrancaHandler);` logo após os gatilhos de follow-up.

- [ ] **Step 3: Rodar, commit**

```bash
pnpm vitest run lib/asaas/consumidor.test.ts lib/event-log
git add lib/asaas/consumidor.ts lib/asaas/consumidor.handler.ts lib/asaas/consumidor.db.ts lib/asaas/consumidor.test.ts lib/event-log/register-handlers.ts
git commit -m "feat(bacco): cobrança vencida matricula o número principal no retorno; paga encerra"
```

---

### Task 8: Cron `asaas-reconcile`

**Files:**
- Create: `lib/asaas/reconcile.ts`, `app/api/v1/cron/asaas-reconcile/route.ts`
- Modify: `docker/scheduler/entrypoint.sh` (linha `0 6 * * *|120|api/v1/cron/asaas-reconcile`, **fora** de comentário com crase), `vercel.ts` (`{ path: "/api/v1/cron/asaas-reconcile", schedule: "0 6 * * *" }`)
- Test: `lib/asaas/reconcile.test.ts`, `app/api/v1/cron/asaas-reconcile/route.test.ts`

- [ ] **Step 1: Testes (falhando)**

```ts
it("org sem integração healthy → pulada");
it("OVERDUE no Asaas sem enrollment viva em asaas_charges → emit_event asaas.payment_overdue com entity_kind asaas_reconcile; com enrollment viva → nada");
it("RECEIVED (paymentDate[ge]=ontem, ou varredura por payment_id se a medição 1 disse que o filtro não existe) ainda OVERDUE em asaas_charges → emit asaas.payment_received");
it("GET /webhooks sem a nossa URL → charge_webhook_paused 1x/dia; interrupted:true → PUT religando + aviso");
it("401 → status='error' + status_reason + aviso; não insiste");
it("rota: sem efeito → audit não chamado; com efeito → audit uma vez com a contagem (gate cron-audita-so-quando-ha-efeito)");
```

- [ ] **Step 2: Implementar** — rota no molde de `webhook-log-retention/route.ts` (auth por `INTERNAL_CRON_SECRET`/`INTERNAL_SECRET`), varre `tenant_integrations where provider='asaas' and status='healthy'`, chama `reconciliarOrg(integ)` de `reconcile.ts`, soma `{ overdue_emitidos, received_emitidos, webhooks_religados, avisos }` e só audita (`action: "cron.asaas_reconcile"` — entra em `AUDIT_ACTIONS`) dentro de `if (total > 0)`.

- [ ] **Step 3: Rodar, commit**

```bash
pnpm vitest run lib/asaas/reconcile.test.ts "app/api/v1/cron/asaas-reconcile" tests/unit/cron-routes-scheduled.test.ts tests/unit/cron-audita-so-quando-ha-efeito.test.ts
git add lib/asaas/reconcile.ts lib/asaas/reconcile.test.ts app/api/v1/cron/asaas-reconcile docker/scheduler/entrypoint.sh vercel.ts lib/audit/actions.ts
git commit -m "feat(bacco): reconciliação diária com o Asaas — vencidas perdidas, pagas perdidas, webhook pausado"
```

---

### Task 9: Empresa ↔ Asaas na tela (cartão, vínculo pelo CNPJ, coluna na lista) e Central com "vincular"

**Files:**
- Create: `components/companies/CartaoAsaas.tsx`, `app/api/v1/companies/[id]/asaas/route.ts` (`GET` pendências ao vivo; `POST` vincular pelo CNPJ)
- Modify: `app/app/companies/[id]/_client.tsx` (cartão só quando `integracoesAtivas` tem `asaas` — a página server passa a flag), `components/companies/CompaniesTable.tsx` (coluna "Asaas" sim/não), `lib/companies/handler.ts` (`asaas_customer_id` já vem no `COLS`), `components/companies/CompanyPicker.tsx` (`useTemEmpresas() || asaasAtivo`)
- Create: `app/api/v1/contacts/[id]/asaas-link/route.ts` (`POST { customer_id }`, papel `agent`, chama `vincularPeloOperador`, audit `asaas.contact_linked` com `metadata.actor:"operator"`). **Nada de abrir `asaas_customer_id` no PATCH genérico de contatos**: o vínculo é ato declarado, com rota e audit próprios, e o PATCH continuaria sem a conferência de telefone nem o audit certo.
- Modify: `app/app/ai/inbox/_components/AgentInboxList.tsx` (para `charge_unmatched`: botão "Vincular" que abre busca de empresa/contato e chama `POST /api/v1/companies/[id]/asaas` ou `POST /api/v1/contacts/[id]/asaas-link`; o `customer_id` vem do `body` do aviso)
- Test: `app/api/v1/companies/[id]/asaas/route.test.ts`, `components/companies/CartaoAsaas.test.tsx`

- [ ] **Step 1: Testes** — `GET` devolve `PENDING`+`OVERDUE` projetados (sem documento) e 409 `asaas_inativo` quando o módulo está desligado; `POST { cnpj }` busca `customerPorDocumento`, grava `crm_companies.asaas_customer_id`, audit `asaas.company_linked` (`resourceId` = uuid da empresa); CNPJ sem customer → 404 com texto; customer já vinculado a outro cadastro (`23505`) → 409 com texto; `POST /contacts/[id]/asaas-link` com `viewer` → 403, com `agent` → 200 e audit; cartão some quando `asaasAtivo=false`.
- [ ] **Step 2: Implementar, dicionário, gates, commit**

```bash
git commit -m "feat(bacco): empresa vinculada ao Asaas pela tela; pendências ao vivo; aviso da Central com botão de vincular"
```

---

### Task 10: Mapa, fragmento, docs, gate aditivo

**Files:**
- Create: `docs/architecture/asaas.architecture.json` (lanes `tela`, `agente`, `eventos`, `dados`, `asaas`; nodes: tela-integracao, tela-empresa, tools-cobranca, pickTools-filtro, rota-webhook, event_log, consumidor, cron-reconcile, followup, central, tenant_integrations, asaas_charges, crm_companies, api-asaas; ≥ 2 arestas por peça nova; `mainPath`: rota-webhook → event_log → consumidor → followup)
- Create: `.changes/bacco-asaas.md` (`impacto: capacidade_nova`, `secao: adicionado`, título "Integração com o Asaas: o assistente consulta e prorroga cobranças; cobrança vencida avisa o número principal")
- Modify: `docs/superpowers/specs/2026-09-17-bacco-asaas-design.md` (estado → "implementado; medições em asaas-sandbox-medido-crm.md"), `docs/testing/user-journey-map.md` (jornada "Cobrança vencida → retorno → pagamento", P1)
- Verify: `tests/unit/empresas-sao-aditivas.test.ts` continua verde (nada de `lib/companies` fora do permitido — `lib/asaas` está na allowlist; `lib/mcp/tools/cobranca.ts` importa `lib/asaas`, não `lib/companies`)

```bash
pnpm vitest run tests/unit/mapas-de-arquitetura.test.ts tests/unit/empresas-sao-aditivas.test.ts; pnpm release:conferir
git commit -m "docs(bacco): mapa, fragmento e jornada do módulo Asaas"
```

---

### Task 11: Prova na VPS (sandbox real) e fechamento

**Files:**
- Create: `tests/e2e/asaas.spec.ts` (+ `FORA_DO_CI` com motivo "exige sandbox Asaas e webhook público; roda na VPS")
- Evidence: `.superpowers/evidence/asaas/`

- [ ] **Step 1: Preparar** na VPS: banco fresco do `baseline.sql` + `bootstrap-owner.ts`; `next build && next start`; WhatsApp de teste conectado; agente Lina publicada com o pacote `reter`.
- [ ] **Step 2: Jornada pela tela** (Playwright, screenshot em cada marco):
  1. Admin abre Integrações › Asaas, cola a chave de **sandbox**, ambiente sandbox, dias=5, máx=1, escolhe o fluxo "Cobrança vencida" (criar antes no editor de retorno: gatilho webhook, nó `ai_message` com hint "consulte as pendências e ofereça o boleto ou uma nova data"); tela recusa salvar até Lina ter as 3 capacidades; liga as capacidades em Lina, publica, salva; Testar conexão → saldo; Ativar → URL + token uma vez.
  2. Cadastra URL + token no painel do sandbox (manual; evidência = screenshot do painel).
  3. Empresas: cria "Adega Teste", vincula 2 contatos (os 2 WhatsApps de teste), marca o primeiro como principal; cartão Asaas → "Vincular pelo CNPJ" com o CNPJ do customer criado na Task 0 → pendências ao vivo aparecem.
  4. Cria no sandbox uma cobrança BOLETO com `dueDate` no passado para esse customer (curl, evidência no arquivo de medição). Se o sandbox não vence na hora, roda `curl` no cron `asaas-reconcile` com o `INTERNAL_SECRET`.
  5. Espera o webhook (ou o cron) → `webhook_events_log` tem a linha (sem `cpfCnpj` no `raw_body`, sem `asaas-access-token` em `headers`), `event_log` consumido, `asaas_charges.enrollment_id` preenchido, **só o principal** recebeu a mensagem da Lina no WhatsApp (o segundo contato não).
  6. Pelo segundo WhatsApp, o cliente escreve "quero pagar o boleto" → Lina consulta (`crm_list_contact_charges`) e manda a linha digitável.
  7. Pelo principal: "não consigo pagar hoje, dá pra adiar?" → Lina prorroga (`crm_reissue_overdue_charge`), manda o boleto novo; segunda tentativa → recusa por limite e oferece atendente.
  8. `receiveInCash` no sandbox → webhook `PAYMENT_RECEIVED` → matrícula cancelada `charge_settled`, atividade "Cobrança paga" na timeline do principal.
  9. Desativar módulo com uma matrícula viva (criar outra vencida antes) → confirmação com "1 retorno será encerrado" → matrícula cancelada `asaas_disabled`; capacidades somem do turno (mensagem ao agente não consulta mais; provar com um turno).
- [ ] **Step 3: Suíte e gates**

```bash
rm -f tsconfig*.tsbuildinfo; pnpm typecheck; echo exit=$?
pnpm lint; echo exit=$?; pnpm lint:channels; echo exit=$?
pnpm test:unit > /tmp/vt.log 2>&1; echo exit=$?; grep -aE "Test Files|Tests |Errors " /tmp/vt.log | tail -3
pnpm build; echo exit=$?
pnpm test:db; echo exit=$?      # VPS
pnpm test:shell; echo exit=$?   # tocou o entrypoint do scheduler
```

- [ ] **Step 4: Sabotagem** (uma por peça crítica, antes de declarar pronto): (a) comentar o `continue` de `requerIntegracao` em `pickToolsFromMcp` → `ponte-nao-monta-integracao-desligada` vermelho; (b) trocar `timingSafeEqual` por `===` → o caso do spy (Task 6) fica vermelho; (c) remover o `where provider='asaas'` do índice único → `asaas.test.ts` caso "generic continua passando" vermelho; (d) inverter `p.customer !== titular.customerId` → `mcp-cobranca-tools` caso "outro customer" vermelho. Restaurar tudo.

- [ ] **Step 5: Commit + runbook no vault**

```bash
git add tests/e2e/asaas.spec.ts .github/workflows/e2e.yml .superpowers/evidence/asaas
git commit -m "test(bacco): prova do módulo Asaas na VPS com sandbox real"
```

Vault: `projetos/bacco-crm/runbooks/asaas.md` com o que só se descobre operando (webhook manual, `interrupted`, sandbox não vence sozinho, `paymentDate[ge]` existe ou não, o que fazer quando `charge_webhook_paused` aparece), `ultima_revisao` de hoje.

---

## Self-review

- **Cobertura da spec:** §5.1 tela/actions (T5), §5.2 schema (T1), §5.2a cartão/coluna/vincular (T9), §5.3 titular e documento não persistido (T3), §6 ferramentas + filtro (T4), §7 webhook (T6), §8 consumidor (T7), §9 cron (T8), §10 Central/navegação/mapa (T1, T5, T9, T10), §11 desativar cancela matrículas (T5), §12 testes (cada task + T11), §13 entrega (T10, T11). Medições de §3 (T0).
- **Nomes consistentes entre tasks:** `carregarIntegracaoAsaas`, `carregarCapacidadesDeIntegracao` (set com `asaas` / `asaas:reemitir`), `crm_link_contact_to_billing`, `vincularPeloOperador`, `chaveDeAviso`, `AsaasCliente`, `AsaasErro`, `titularDoContato`, `titularPorCustomer`, `vincularContatoPorDocumento`, `validarFluxoDeCobranca`, `asaasCobrancaHandler`, `TOOLS_COBRANCA`, kinds `charge_*`, atividades `charge_*`, ações `asaas.*`, `cancel_reason` `charge_settled`/`asaas_disabled`.
- **Onde o plano depende de medição:** Zod do envelope (T6), filtro `paymentDate[ge]` (T8), 404 vs 200 (T6), Pix em BOLETO (T4) — todos apontam para a Task 0.
- **Placeholders:** os testes das Tasks 3, 5, 7, 8 e 9 estão em forma de `it("...")` com o comportamento esperado descrito; o executor escreve o corpo com os fakes já definidos nas Tasks 2 e 4 (`fakeSb`, `fakeSupabase`/`ctxDe`, `ConsumidorDb` em memória). Nenhum "TBD".
